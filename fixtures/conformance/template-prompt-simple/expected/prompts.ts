// @generated by @metaobjectsdev/codegen-ts — hand edits are kept by a three-way merge on regeneration; meta verify --codegen checks only the generated parts.

import { render, type Provider } from "@metaobjectsdev/render";
import type { NpcPromptPayload } from "./NpcPromptPayload.js";

export function renderNpcTurn(payload: NpcPromptPayload, provider: Provider): string {
  return render({ ref: "npc/turn", payload, format: "xml", provider });
}
