// @generated by scripts/generate-requirement-harness.ts — DO NOT EDIT IN PLACE.
//
// One slot per authored promise in metaobjects/meta.requirements.yaml (109 of them).
// Every slot is SKIPPED: this is a to-do list with a drift gate behind it, not a
// claim that nothing is verified — the conformance corpora already check many of
// these. To FILL one, move it into C#'s own suite and write the assertion the
// skip reason describes. Adding a promise to the ledger adds a slot here.
using Xunit;

namespace MetaObjects.Requirements;

public class RequirementHarnessTests
{

    // Boolean attribute — A boolean attribute carries exactly true or false, and text that merely looks like one is not one.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: The word false read as a value that is present, so a flag written to switch something off switches it on.")]
    public void declare_attributeValueTypes_attrBoolean() { /* metaobjects.declare.attributeValueTypes.attrBoolean */ }

    // Type-name attribute — A type-name attribute carries the name of a type to be bound when code is generated, never resolved by reflection while running.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A type looked up from a string at run time, so the model works under one runtime and fails under a compiled one.")]
    public void declare_attributeValueTypes_attrClass() { /* metaobjects.declare.attributeValueTypes.attrClass */ }

    // Fractional attribute — A fractional attribute carries a non-integer number on a declaration.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A ratio written as a fraction and read as a whole number, so a half becomes nothing.")]
    public void declare_attributeValueTypes_attrDouble() { /* metaobjects.declare.attributeValueTypes.attrDouble */ }

    // Expression attribute — An expression attribute carries an expression whose text is the author's and whose placement in the model is the metamodel's.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A derived column's formula living inside a generator instead of the model, so changing it regenerates nothing.")]
    public void declare_attributeValueTypes_attrExpression() { /* metaobjects.declare.attributeValueTypes.attrExpression */ }

    // Filter attribute — A filter attribute carries a declared predicate as structure, not as a fragment of query language.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A scoping condition written as a string of SQL, meaning one thing on one engine and failing to parse on another.")]
    public void declare_attributeValueTypes_attrFilter() { /* metaobjects.declare.attributeValueTypes.attrFilter */ }

    // Whole-number attribute — A whole-number attribute carries a signed 32-bit integer on a declaration.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A length limit written as text, so a limit of 128 compares as smaller than a limit of 64.")]
    public void declare_attributeValueTypes_attrInt() { /* metaobjects.declare.attributeValueTypes.attrInt */ }

    // Symbol-to-integer map — A symbol-to-integer map attribute carries an explicit mapping from declared symbols to numbers, so the correspondence survives the symbols being reordered.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A set of members mapped to storage by position, where inserting one member silently re-labels every row already written.")]
    public void declare_attributeValueTypes_attrIntMap() { /* metaobjects.declare.attributeValueTypes.attrIntMap */ }

    // Large whole-number attribute — A large whole-number attribute carries a signed 64-bit integer on a declaration.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A bound above the 32-bit range quietly narrowed at load, so the declaration means less than it says.")]
    public void declare_attributeValueTypes_attrLong() { /* metaobjects.declare.attributeValueTypes.attrLong */ }

    // Free-form properties — A free-form properties attribute carries author- supplied key-value data that the metamodel deliberately does not interpret, so a project can record something of its own without inventing vocabulary.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A project needing one note of its own on a declaration and having to choose between an attribute the loader refuses and switching the refusal off.")]
    public void declare_attributeValueTypes_attrProperties() { /* metaobjects.declare.attributeValueTypes.attrProperties */ }

    // Text attribute — A text attribute carries characters on a declaration and is checked as text when the model loads.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A number written where a name belongs, coerced to text, and emitted into generated code as a name.")]
    public void declare_attributeValueTypes_attrString() { /* metaobjects.declare.attributeValueTypes.attrString */ }

    // Element count — An element-count rule bounds how many entries a repeated value may hold.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: An unbounded list accepted into a field whose reader only ever looks at the first few.")]
    public void declare_constraints_validatorArray() { /* metaobjects.declare.constraints.validatorArray */ }

    // At least one — An at-least-one rule requires that some member of a declared group is given.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A contact saved with neither an email address nor a phone number, and no way to reach them.")]
    public void declare_constraints_validatorAtLeastOne() { /* metaobjects.declare.constraints.validatorAtLeastOne */ }

    // Comparison — A comparison rule relates one declared value to another on the same object.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: An end date before its start date, stored, and every report that spans them silently empty.")]
    public void declare_constraints_validatorComparison() { /* metaobjects.declare.constraints.validatorComparison */ }

    // Length bounds — A length rule bounds how long a value may be, and an explicitly declared bound is authoritative over any implicit floor.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A declared minimum of zero raised to one behind the author's back, so provided-but-empty becomes inexpressible.")]
    public void declare_constraints_validatorLength() { /* metaobjects.declare.constraints.validatorLength */ }

    // Minimum length — A declared minimum is authoritative and is never widened by an implicit floor, so zero means provided-and-possibly-empty and is able to say so.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A minimum of zero raised to one behind the author's back, making required-but-empty inexpressible.")]
    public void declare_constraints_validatorLength_min() { /* metaobjects.declare.constraints.validatorLength.min */ }

    // Numeric bounds — A numeric rule bounds the range a number may take.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A negative quantity accepted, so an order adds to stock instead of taking from it.")]
    public void declare_constraints_validatorNumeric() { /* metaobjects.declare.constraints.validatorNumeric */ }

    // Present together — A present-together rule ties two values so that either both are given or neither is.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A latitude stored with no longitude, and a point that cannot be placed.")]
    public void declare_constraints_validatorPresentIff() { /* metaobjects.declare.constraints.validatorPresentIff */ }

    // Pattern — A pattern rule constrains a value to a declared expression, matched in FULL rather than anywhere within it.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A pattern meant to accept a postcode accepting any text that contains one.")]
    public void declare_constraints_validatorRegex() { /* metaobjects.declare.constraints.validatorRegex */ }

    // Required — A required rule says a value must be provided, and for text that emptiness is not providing it.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: An empty string accepted for a name, so a record exists with nobody's name on it.")]
    public void declare_constraints_validatorRequired() { /* metaobjects.declare.constraints.validatorRequired */ }

    // Conditionally required — A conditional-requirement rule makes a value mandatory only in a declared circumstance.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A cancellation reason demanded on every order, or on none of them.")]
    public void declare_constraints_validatorRequiredWhen() { /* metaobjects.declare.constraints.validatorRequiredWhen */ }

    // Output template — An output template declares a rendering that only ever goes outward. Its axis is DIRECTION, so it has no inbound tier and emits no parser.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: An outbound-only template given a parser for a response it will never receive.")]
    public void declare_declaredPrompts_templateOutput() { /* metaobjects.declare.declaredPrompts.templateOutput */ }

    // Prompt template — A prompt template declares an outbound message to a model: the payload it carries, and — when it expects a structured reply — the shape that reply must take.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A prompt whose payload is assembled somewhere else, so nothing can tell whether a renamed field still reaches it.")]
    public void declare_declaredPrompts_templatePrompt() { /* metaobjects.declare.declaredPrompts.templatePrompt */ }

    // Declared payload — A payload reference names the declared shape a prompt sends, which is what makes what-it- sends a fact about the model and payload growth a diff.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A prompt assembling its payload from whatever the caller passed, so nobody can say what it sends without running it.")]
    public void declare_declaredPrompts_templatePrompt_payloadRef() { /* metaobjects.declare.declaredPrompts.templatePrompt.payloadRef */ }

    // Declared response — A response reference is what makes a prompt a RESPONDING prompt: it declares the shape a reply must take, and the parser, the tolerant extractor and the response-format fragment all key off it.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A reply parsed against a shape the prompt never asked for.")]
    public void declare_declaredPrompts_templatePrompt_responseRef() { /* metaobjects.declare.declaredPrompts.templatePrompt.responseRef */ }

    // External prompt text — A text reference keeps a prompt's words outside the model and resolved by the provider, so changing what a prompt SAYS is not a change to the model.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: Prompt wording embedded in the declaration, making every rewording a metadata change and a regeneration.")]
    public void declare_declaredPrompts_templatePrompt_textRef() { /* metaobjects.declare.declaredPrompts.templatePrompt.textRef */ }

    // Tool call template — A tool-call template declares a tool a model may invoke by name, with a declared payload and a declared budget for the call.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A tool a model can call whose arguments nothing declares, so a wrong shape is discovered by the tool itself.")]
    public void declare_declaredPrompts_templateToolcall() { /* metaobjects.declare.declaredPrompts.templateToolcall */ }

    // Aggregate — An aggregate declares a value computed over related rows, optionally scoped, and derived rather than stored.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A count kept in a column and maintained by hand, disagreeing with the rows it counts.")]
    public void declare_derivation_originAggregate() { /* metaobjects.declare.derivation.originAggregate */ }

    // Aggregation — The aggregation says what is computed over the related rows — a count, a sum, an extreme, a whole-row rollup — and the result is derived, never stored.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A total maintained by application code, drifting from the rows it totals with nothing to notice.")]
    public void declare_derivation_originAggregate_agg() { /* metaobjects.declare.derivation.originAggregate.agg */ }

    // Aggregate scope — A scoping filter narrows which related rows an aggregate counts, as declared structure rather than as a fragment of query language.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: An open-orders count that includes the closed ones, because the scoping lived in a hand-written view nobody regenerated.")]
    public void declare_derivation_originAggregate_filter() { /* metaobjects.declare.derivation.originAggregate.filter */ }

    // What is aggregated — The of attribute names what is aggregated; omitting it on a collecting aggregate means each related row is collected AS the carrying field's declared value object.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A rollup returning the whole entity where the declaration asked for a curated shape, because the target was inferred instead of read.")]
    public void declare_derivation_originAggregate_of() { /* metaobjects.declare.derivation.originAggregate.of */ }

    // Path to the rows — The via attribute names the path from the base object to what is aggregated, and may be left out only when exactly one single-hop relationship leads there.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: Two relationships reaching the same object and the aggregate silently picking one of them.")]
    public void declare_derivation_originAggregate_via() { /* metaobjects.declare.derivation.originAggregate.via */ }

    // Computed value — A computed value declares a value produced by a declared expression over the row it sits on.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A derived figure written directly, so it stops matching the inputs it was derived from.")]
    public void declare_derivation_originComputed() { /* metaobjects.declare.derivation.originComputed */ }

    // First of many — A first-of-many declares a value taken from one related row, chosen by a declared order rather than by chance.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A latest value that is whichever row the database happened to return.")]
    public void declare_derivation_originFirst() { /* metaobjects.declare.derivation.originFirst */ }

    // Passthrough — A passthrough declares that a value IS a value on another object, and must keep its type across the hop.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A number carried through as text because the two ends disagreed about its type, with nothing saying so.")]
    public void declare_derivation_originPassthrough() { /* metaobjects.declare.derivation.originPassthrough */ }

    // Deliberate type change — The convert flag is the explicit acknowledgement that a passthrough changes type. Without it a mismatch across the hop fails the load rather than being coerced.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A number carried through into a text field with nothing recording that anyone meant it to convert.")]
    public void declare_derivation_originPassthrough_convert() { /* metaobjects.declare.derivation.originPassthrough.convert */ }

    // Source of the value — The from attribute names the value this one IS, on the object it comes from.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A field that looks like a copy and is a re- implementation, so the two drift apart and nothing compares them.")]
    public void declare_derivation_originPassthrough_from() { /* metaobjects.declare.derivation.originPassthrough.from */ }

    // Architectural requirement — An architectural requirement states a policy that must hold UNIVERSALLY, and its check is universality: it fails when something is outside it.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A rule declared once, applied to nothing, with every gate reporting green.")]
    public void declare_governanceVocabulary_requirementArchitectural() { /* metaobjects.declare.governanceVocabulary.requirementArchitectural */ }

    // Functional requirement — A functional requirement states a capability that must EXIST, and its check is existence: it fails when nothing implements it.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A capability everyone believes is there, that nothing builds and no check misses.")]
    public void declare_governanceVocabulary_requirementFunctional() { /* metaobjects.declare.governanceVocabulary.requirementFunctional */ }

    // Link to the model — The implementedBy attribute names the model nodes realising a requirement, and is legal only where a requirement addresses the model at all — at an object or one of its members, never at an organisational tier.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A solution-level entry holding a list of every entity, so the tier that exists to delegate is also the tier that holds every link.")]
    public void declare_governanceVocabulary_requirementFunctional_implementedBy() { /* metaobjects.declare.governanceVocabulary.requirementFunctional.implementedBy */ }

    // Model document — A model document is what declarations are written in. It names the package they belong to, which is what lets a bare reference bind locally and a qualified one bind exactly.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: One name declared in two packages and a reference to it binding to whichever was read first.")]
    public void declare_inheritanceAndReferences_metadataRoot() { /* metaobjects.declare.inheritanceAndReferences.metadataRoot */ }

    // Entity — An entity owns its data: it has its own identity, a writable place for that data to live, and a lifecycle.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A record that cannot be updated, because nothing declares where it lives or which values identify one of it.")]
    public void declare_objectTaxonomy_objectEntity() { /* metaobjects.declare.objectTaxonomy.objectEntity */ }

    // Projection — A projection is a derived read-only representation whose declared field set IS its exposure: an inclusive list that fails closed.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A view that starts returning a column nobody listed, because the table underneath it gained one.")]
    public void declare_objectTaxonomy_objectProjection() { /* metaobjects.declare.objectTaxonomy.objectProjection */ }

    // Row scope — A projection's row filter declares which rows the projection exposes at all, as part of the projection rather than as something every caller must remember to add.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A view meant to show active records showing every record, because scoping was left to each query.")]
    public void declare_objectTaxonomy_objectProjection_filter() { /* metaobjects.declare.objectTaxonomy.objectProjection.filter */ }

    // Value object — A value object is pure shape — never its own identity, never its own source — constructed by whoever embeds it rather than fetched.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A transport shape that has acquired a primary key, so two copies of one message are now two records.")]
    public void declare_objectTaxonomy_objectValue() { /* metaobjects.declare.objectTaxonomy.objectValue */ }

    // Primary key — A primary key declares which values identify one row and how a new row gets its identifier.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: Two rows nothing can tell apart, because nothing declared what makes one row one row.")]
    public void declare_persistenceMapping_identityPrimary() { /* metaobjects.declare.persistenceMapping.identityPrimary */ }

    // Key generation — The generation attribute says where a new row's key comes from — the database, the writer, or nowhere, which means the caller must supply it.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A key the caller must supply, treated as optional by the create shape, so the insert reaches the database with nothing in it.")]
    public void declare_persistenceMapping_identityPrimary_generation() { /* metaobjects.declare.persistenceMapping.identityPrimary.generation */ }

    // Reference — A reference declares that a value points at another object's key, and what becomes of it when that object goes.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A parent deleted and its children left pointing at nothing, because the reference declared no action.")]
    public void declare_persistenceMapping_identityReference() { /* metaobjects.declare.persistenceMapping.identityReference */ }

    // Constraint or navigation — Enforcement declares whether a reference is a database constraint or a navigational fact, so a shape can point at an entity without acquiring a foreign key.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A message referring to an order by its number, given a foreign key, so the message cannot be stored before the order it describes.")]
    public void declare_persistenceMapping_identityReference_enforce() { /* metaobjects.declare.persistenceMapping.identityReference.enforce */ }

    // Delete action — The delete action declares what becomes of a referencing row when what it references goes, and an unconfigured reference does nothing rather than whatever the framework underneath would have chosen.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A parent deleted and its children silently deleted with it, because a framework supplied a cascade nobody declared.")]
    public void declare_persistenceMapping_identityReference_onDelete() { /* metaobjects.declare.persistenceMapping.identityReference.onDelete */ }

    // Reference target — A reference names the object and key it points at, and that target is resolved when the model loads rather than when a query runs.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A foreign key pointing at something renamed months ago, discovered at the first insert.")]
    public void declare_persistenceMapping_identityReference_references() { /* metaobjects.declare.persistenceMapping.identityReference.references */ }

    // Alternate key — An alternate key declares a second unique way to address a row; its uniqueness comes from being that type, never from an attribute somebody remembered to set.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: Two customers sharing an email address, because the index meant to prevent it was never unique.")]
    public void declare_persistenceMapping_identitySecondary() { /* metaobjects.declare.persistenceMapping.identitySecondary */ }

    // Lookup index — A lookup index declares a non-unique retrieval path. Uniqueness lives in the TYPE, so an index cannot claim it by accident.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A retrieval index that silently enforces uniqueness, so the second row with the same value is refused.")]
    public void declare_persistenceMapping_indexLookup() { /* metaobjects.declare.persistenceMapping.indexLookup */ }

    // Expression key — An expression index declares that what is indexed is computed rather than stored, and it is EXCLUSIVE with naming fields — one key or the other, never a silent half.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: An index declaring both a field list and an expression, where half of it is quietly discarded and the index built is not the index described.")]
    public void declare_persistenceMapping_indexLookup_expr() { /* metaobjects.declare.persistenceMapping.indexLookup.expr */ }

    // Relational source — A relational source says where an object's data lives, in what kind of place, and in what role, without naming a database engine.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A model that only reveals which engine it assumed when a migration fails against a different one.")]
    public void declare_persistenceMapping_sourceRdb() { /* metaobjects.declare.persistenceMapping.sourceRdb */ }

    // Kind of place — The kind attribute says what sort of place data lives in, and read-only-ness is DERIVED from it rather than declared a second time alongside it.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A view marked writable, so generated code offers an update the database will refuse.")]
    public void declare_persistenceMapping_sourceRdb_kind() { /* metaobjects.declare.persistenceMapping.sourceRdb.kind */ }

    // Read or write role — The role attribute separates the one place an object is written from the places it is read, and exactly one source is the primary.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: Two sources both claiming to be where writes go, and a row landing in whichever the code reached first.")]
    public void declare_persistenceMapping_sourceRdb_role() { /* metaobjects.declare.persistenceMapping.sourceRdb.role */ }

    // Hand-written body — A hand-written body declares SQL the model cannot express, and taking that escape hatch means owning the result: nothing derives it and nothing checks it back against the model.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A hand-written view whose columns no longer match the projection naming it, with every generated artifact still built from the projection.")]
    public void declare_persistenceMapping_sourceRdb_sql() { /* metaobjects.declare.persistenceMapping.sourceRdb.sql */ }

    // Not ours to change — The unmanaged marker declares that a database object is not the toolchain's to create or alter, so a migration proposes nothing for it.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A hand-maintained view rewritten by a migration that assumed it owned it.")]
    public void declare_persistenceMapping_sourceRdb_unmanaged() { /* metaobjects.declare.persistenceMapping.sourceRdb.unmanaged */ }

    // Data grid — A data grid declares which of an object's fields a tabular view shows, in what order, and how it is sorted and paged before anyone touches it.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A table that gains a column every time the model does, including the ones nobody meant to show.")]
    public void declare_presentation_layoutDataGrid() { /* metaobjects.declare.presentation.layoutDataGrid */ }

    // Shown columns — The column list declares what a tabular view shows and in what order, INCLUSIVELY — so a field added to the model does not appear in a grid by having been added.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: An internal field appearing in a customer- facing table the day it was added to the model.")]
    public void declare_presentation_layoutDataGrid_columns() { /* metaobjects.declare.presentation.layoutDataGrid.columns */ }

    // Checkbox — A checkbox declares that a boolean is set by a single on-or-off control.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A yes-or-no answer offered as a text box, so y, yes and true all arrive and all mean the same thing.")]
    public void declare_presentation_viewCheckbox() { /* metaobjects.declare.presentation.viewCheckbox */ }

    // Money display — A money display declares that an amount is shown in the conventions of a declared locale, formatted where it is read and never on the server.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: An amount formatted once on the server for one locale and shown to a reader in another.")]
    public void declare_presentation_viewCurrency() { /* metaobjects.declare.presentation.viewCurrency */ }

    // Formatting locale — The locale says which conventions an amount is formatted in, and formatting happens where the value is read rather than where it is stored.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: An amount formatted once on the server for one reader's conventions and shown to another's.")]
    public void declare_presentation_viewCurrency_locale() { /* metaobjects.declare.presentation.viewCurrency.locale */ }

    // Date picker — A date control declares that a value is chosen from a calendar.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A date typed as free text, so every reader invents their own format and half of them are ambiguous.")]
    public void declare_presentation_viewDate() { /* metaobjects.declare.presentation.viewDate */ }

    // Dropdown — A dropdown declares that a value is chosen from its declared members in a collapsed list.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A closed set of members offered as free text, so a typo silently becomes a new value.")]
    public void declare_presentation_viewDropdown() { /* metaobjects.declare.presentation.viewDropdown */ }

    // Hidden value — A hidden control declares that a value travels with a form without being shown or edited.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A value the form has to carry, re-fetched on submit because there was no way to carry it.")]
    public void declare_presentation_viewHidden() { /* metaobjects.declare.presentation.viewHidden */ }

    // Link — A link control declares that a value is shown as a link to whatever it addresses.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: An address displayed as text nobody can follow without selecting and copying it.")]
    public void declare_presentation_viewHotlink() { /* metaobjects.declare.presentation.viewHotlink */ }

    // Image upload — An image control declares that a value is an image a person uploads and crops, stored as an opaque key so no image bytes ever cross the model.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A record carrying its own image bytes, so every list of records carries every picture in it.")]
    public void declare_presentation_viewImage() { /* metaobjects.declare.presentation.viewImage */ }

    // Month picker — A month control declares that a value is chosen as a month within a year, with no day.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A billing period entered as a full date, so twelve possible values become three hundred and sixty- five.")]
    public void declare_presentation_viewMonth() { /* metaobjects.declare.presentation.viewMonth */ }

    // Number input — A number control declares that a value is entered as a number rather than as text that must later parse.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A quantity typed with a thousands separator and refused by the server instead of by the field.")]
    public void declare_presentation_viewNumber() { /* metaobjects.declare.presentation.viewNumber */ }

    // Masked input — A masked control declares that a value is entered without being displayed.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A secret typed in plain sight and left legible in a screen recording.")]
    public void declare_presentation_viewPassword() { /* metaobjects.declare.presentation.viewPassword */ }

    // Radio group — A radio group declares that a value is chosen from its declared members with every option visible at once.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A three-way choice hidden behind a control that shows one option until someone opens it.")]
    public void declare_presentation_viewRadio() { /* metaobjects.declare.presentation.viewRadio */ }

    // Single-line text — A single-line text control declares that a value is edited as one line.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A short label given a box the height of a paragraph, because the interface had only the type to guess from.")]
    public void declare_presentation_viewText() { /* metaobjects.declare.presentation.viewText */ }

    // Multi-line text — A multi-line text control declares that a value is edited across several lines, to a declared visible height.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A paragraph edited through a one-line control, so the author cannot see what they have written.")]
    public void declare_presentation_viewTextarea() { /* metaobjects.declare.presentation.viewTextarea */ }

    // Web-rendered view base — An abstract base groups the views that render in a browser, so what they share is declared once rather than on each of them.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A property common to every browser-rendered view restated on each one, where adding a view means remembering to restate it again.")]
    public void declare_presentation_viewWeb() { /* metaobjects.declare.presentation.viewWeb */ }

    // Aggregation — An aggregation declares that one object groups others which outlive it.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A team disbanded and its members deleted along with it.")]
    public void declare_relationships_relationshipAggregation() { /* metaobjects.declare.relationships.relationshipAggregation */ }

    // Association — An association declares that two objects refer to one another, with neither owning the other's lifetime.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: Deleting a category taking every product in it, because a reference was modelled as ownership.")]
    public void declare_relationships_relationshipAssociation() { /* metaobjects.declare.relationships.relationshipAssociation */ }

    // Cardinality — Cardinality says whether one side reaches one row or many, and it is what decides the shape of everything generated from the relationship.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A one-to-many read as one-to-one, so a customer's second order is unreachable.")]
    public void declare_relationships_relationshipAssociation_cardinality() { /* metaobjects.declare.relationships.relationshipAssociation.cardinality */ }

    // Junction entity — A through-entity names the junction a many- to-many passes over, and the relationship's keys are DERIVED from that junction's own references rather than restated here.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A join table's key direction stated twice — once on the junction, once on the relationship — and the two disagreeing.")]
    public void declare_relationships_relationshipAssociation_through() { /* metaobjects.declare.relationships.relationshipAssociation.through */ }

    // Composition — A composition declares that one object owns another's lifetime, so the parent going takes the children with it.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: An order deleted and its lines left behind, referenced by nothing and still counted by every report.")]
    public void declare_relationships_relationshipComposition() { /* metaobjects.declare.relationships.relationshipComposition */ }

    // Boolean — A boolean field holds exactly true or false, and never a third state standing in for absent.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: An unanswered question stored as false, so nobody can tell it apart from an answer of no.")]
    public void declare_typedFieldVocabulary_fieldBoolean() { /* metaobjects.declare.typedFieldVocabulary.fieldBoolean */ }

    // Money — A money field holds an amount as a whole number of the minor units of a declared currency, and is never handled as a fraction.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A price held as a binary fraction, where adding a hundred of them is off by a cent.")]
    public void declare_typedFieldVocabulary_fieldCurrency() { /* metaobjects.declare.typedFieldVocabulary.fieldCurrency */ }

    // Currency code — The currency attribute names which currency an amount is in, and fixes what one minor unit of it means for that field.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A yen amount divided by a hundred because whoever read it assumed two decimal places.")]
    public void declare_typedFieldVocabulary_fieldCurrency_currency() { /* metaobjects.declare.typedFieldVocabulary.fieldCurrency.currency */ }

    // Calendar date — A date field holds a day on a calendar — no time, no zone.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A birthday that lands on the previous day for a reader in another region.")]
    public void declare_typedFieldVocabulary_fieldDate() { /* metaobjects.declare.typedFieldVocabulary.fieldDate */ }

    // Exact decimal — An exact-decimal field holds a base-10 number of declared precision and scale, and never passes through binary floating point to get there.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A tax figure that differs by a cent depending on which language computed it.")]
    public void declare_typedFieldVocabulary_fieldDecimal() { /* metaobjects.declare.typedFieldVocabulary.fieldDecimal */ }

    // Double-precision number — A double-precision field holds a binary floating- point number, with the inexactness that implies stated rather than discovered.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A running total accumulated in one, where the sum of the parts no longer equals the whole.")]
    public void declare_typedFieldVocabulary_fieldDouble() { /* metaobjects.declare.typedFieldVocabulary.fieldDouble */ }

    // Enumeration — An enumeration field holds one member of a closed declared set, and the member symbol is what the wire and the runtime see whatever the column stores.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A stored value that is no declared member, handed back as though it were one.")]
    public void declare_typedFieldVocabulary_fieldEnum() { /* metaobjects.declare.typedFieldVocabulary.fieldEnum */ }

    // Explicit integer coding — An integer coding fixes each member's stored number EXPLICITLY, so reordering the members cannot re-label rows already written.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A member inserted into the middle of the list, and every stored row now meaning the member after the one it meant yesterday.")]
    public void declare_typedFieldVocabulary_fieldEnum_intValueMap() { /* metaobjects.declare.typedFieldVocabulary.fieldEnum.intValueMap */ }

    // Incoming-value normalisation — Normalisation says how loosely an incoming spelling may match a member before it is accepted.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: Two members whose symbols differ only by a separator, where a delimited value silently matches the wrong one and is reported as extracted rather than malformed.")]
    public void declare_typedFieldVocabulary_fieldEnum_normalize() { /* metaobjects.declare.typedFieldVocabulary.fieldEnum.normalize */ }

    // Member set — The member list IS the legal set, and nothing outside it is a legal value at any tier.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A member added to a database constraint and not to the model, so one path accepts it and every other refuses it.")]
    public void declare_typedFieldVocabulary_fieldEnum_values() { /* metaobjects.declare.typedFieldVocabulary.fieldEnum.values */ }

    // Single-precision number — A single-precision field holds a narrower binary floating-point number, for values whose provenance is already single-precision.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A value stored at one precision and read back at another, reported as changed by a check that compared the two.")]
    public void declare_typedFieldVocabulary_fieldFloat() { /* metaobjects.declare.typedFieldVocabulary.fieldFloat */ }

    // IP address — An address field holds an IP address literal, and a name is never resolved into one on its behalf.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A hostname turned into an address by a lookup while the request was being served, so what got stored depends on when it was stored.")]
    public void declare_typedFieldVocabulary_fieldInet() { /* metaobjects.declare.typedFieldVocabulary.fieldInet */ }

    // Strictness opt-out — Leniency on an address field opts out of requiring a literal AND changes the column it is stored in, so it is a schema decision rather than a validation preference.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A field relaxed to accept anything, still typed as an address underneath, refusing the very values it was relaxed for.")]
    public void declare_typedFieldVocabulary_fieldInet_lenient() { /* metaobjects.declare.typedFieldVocabulary.fieldInet.lenient */ }

    // Whole number — A whole-number field holds a signed 32-bit integer, in every port and on the wire.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A count that wraps to a negative number on one port and not on another.")]
    public void declare_typedFieldVocabulary_fieldInt() { /* metaobjects.declare.typedFieldVocabulary.fieldInt */ }

    // Large whole number — A large whole-number field holds a signed 64-bit integer and crosses the wire without a floating- point conversion in between.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: An identifier above two to the fifty-third arriving in a browser rounded to a different identifier.")]
    public void declare_typedFieldVocabulary_fieldLong() { /* metaobjects.declare.typedFieldVocabulary.fieldLong */ }

    // Map — A map field holds key-to-value pairs whose keys are not known when the model is written.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: An open-ended set of properties modelled as one column per key anyone thought of, so adding a key is a migration.")]
    public void declare_typedFieldVocabulary_fieldMap() { /* metaobjects.declare.typedFieldVocabulary.fieldMap */ }

    // Embedded object — An object field holds a declared value object, stored flattened, as a JSON document, or as a subdocument, according to the declaration.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A structured value kept as opaque text, so nothing can query inside it and nothing checks its shape.")]
    public void declare_typedFieldVocabulary_fieldObject() { /* metaobjects.declare.typedFieldVocabulary.fieldObject */ }

    // Value-object target — An object reference names the value object a field holds, and that target must be a shape — never something carrying its own identity.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A field declaring it holds a shape, pointed at an entity, and generated code embedding a whole table's row inside another.")]
    public void declare_typedFieldVocabulary_fieldObject_objectRef() { /* metaobjects.declare.typedFieldVocabulary.fieldObject.objectRef */ }

    // Physical storage form — The storage attribute says how an embedded object is physically kept — spread across columns, as one queryable document, or as a nested record.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A nested value spread into columns by one port and kept as a document by another, so neither can read the other's rows.")]
    public void declare_typedFieldVocabulary_fieldObject_storage() { /* metaobjects.declare.typedFieldVocabulary.fieldObject.storage */ }

    // Text — A text field holds characters up to a declared maximum length, and attaches no meaning to what they spell beyond what a validator adds.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: Text accepted at a length nothing downstream can store, found when the insert fails rather than when it was typed.")]
    public void declare_typedFieldVocabulary_fieldString() { /* metaobjects.declare.typedFieldVocabulary.fieldString */ }

    // Maximum length — A maximum length bounds a text value everywhere it is checked, and where a separate length rule also applies, the stricter of the two wins.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: One limit on the field and a longer one as a rule, with generated code enforcing whichever it happened to read last.")]
    public void declare_typedFieldVocabulary_fieldString_maxLength() { /* metaobjects.declare.typedFieldVocabulary.fieldString.maxLength */ }

    // Recognised text format — A string format constrains text that has a recognised shape but no behaviour of its own — the residue left once everything with a native type became its own field type.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A value with real behaviour validated as a format string, so nothing downstream knows what it is.")]
    public void declare_typedFieldVocabulary_fieldString_stringFormat() { /* metaobjects.declare.typedFieldVocabulary.fieldString.stringFormat */ }

    // Time of day — A time field holds a time of day with no date attached to it.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: An opening hour that cannot be declared without inventing a date to hang it on.")]
    public void declare_typedFieldVocabulary_fieldTime() { /* metaobjects.declare.typedFieldVocabulary.fieldTime */ }

    // Instant — A timestamp field holds an instant, unambiguous across zones, and holds a naive local reading only where the declaration says so.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: Two events whose recorded order changes when a server moves region.")]
    public void declare_typedFieldVocabulary_fieldTimestamp() { /* metaobjects.declare.typedFieldVocabulary.fieldTimestamp */ }

    // Server-stamped — An auto-set timestamp is stamped by the tier that writes it, on create or on update as declared, and a value the caller supplies is ignored rather than honoured.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A caller setting a record's creation time to whatever suits them, so the audit trail records what the client claimed.")]
    public void declare_typedFieldVocabulary_fieldTimestamp_autoSet() { /* metaobjects.declare.typedFieldVocabulary.fieldTimestamp.autoSet */ }

    // Naive local-time opt-out — The local-time flag is the opt-out from instant semantics: absent, the value is an instant; present, it is a wall-clock reading carrying no zone.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A wall-clock opening time shifted by an hour, because it was stored as an instant in a zone nobody declared.")]
    public void declare_typedFieldVocabulary_fieldTimestamp_localTime() { /* metaobjects.declare.typedFieldVocabulary.fieldTimestamp.localTime */ }

    // URI — A URI field holds an absolute, scheme-bearing address, unless the declaration explicitly relaxes that.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: A relative path accepted into a field that everything downstream will try to dereference.")]
    public void declare_typedFieldVocabulary_fieldUri() { /* metaobjects.declare.typedFieldVocabulary.fieldUri */ }

    // Strictness opt-out — Leniency is the declared opt-out from requiring an absolute, scheme-bearing address. Without it strictness is not a per- port judgement call.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: One port accepting a relative address and another refusing it, from one declaration.")]
    public void declare_typedFieldVocabulary_fieldUri_lenient() { /* metaobjects.declare.typedFieldVocabulary.fieldUri.lenient */ }

    // Unique identifier — A unique-identifier field holds a UUID as a value in its own right, not as text that happens to be shaped like one.
    [Fact(Skip = "unfilled slot — write an assertion that fails when: Two rows differing only in the letter case of their identifier, treated as two different rows.")]
    public void declare_typedFieldVocabulary_fieldUuid() { /* metaobjects.declare.typedFieldVocabulary.fieldUuid */ }

}
