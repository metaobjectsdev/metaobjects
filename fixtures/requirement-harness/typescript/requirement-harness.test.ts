// @generated by scripts/generate-requirement-harness.ts — DO NOT EDIT IN PLACE.
//
// One slot per authored promise in metaobjects/meta.requirements.yaml (109 of them).
// Every slot is SKIPPED: this is a to-do list with a drift gate behind it, not a
// claim that nothing is verified — the conformance corpora already check many of
// these. To FILL one, move it into TypeScript's own suite and write the assertion the
// skip reason describes. Adding a promise to the ledger adds a slot here.
import { test } from "bun:test";

// Boolean attribute — A boolean attribute carries exactly true or false, and text that merely looks like one is not one.
test.skip("metaobjects.declare.attributeValueTypes.attrBoolean", () => {
  // unfilled slot — write an assertion that fails when: The word false read as a value that is present, so a flag written to switch something off switches it on.
});

// Type-name attribute — A type-name attribute carries the name of a type to be bound when code is generated, never resolved by reflection while running.
test.skip("metaobjects.declare.attributeValueTypes.attrClass", () => {
  // unfilled slot — write an assertion that fails when: A type looked up from a string at run time, so the model works under one runtime and fails under a compiled one.
});

// Fractional attribute — A fractional attribute carries a non-integer number on a declaration.
test.skip("metaobjects.declare.attributeValueTypes.attrDouble", () => {
  // unfilled slot — write an assertion that fails when: A ratio written as a fraction and read as a whole number, so a half becomes nothing.
});

// Expression attribute — An expression attribute carries an expression whose text is the author's and whose placement in the model is the metamodel's.
test.skip("metaobjects.declare.attributeValueTypes.attrExpression", () => {
  // unfilled slot — write an assertion that fails when: A derived column's formula living inside a generator instead of the model, so changing it regenerates nothing.
});

// Filter attribute — A filter attribute carries a declared predicate as structure, not as a fragment of query language.
test.skip("metaobjects.declare.attributeValueTypes.attrFilter", () => {
  // unfilled slot — write an assertion that fails when: A scoping condition written as a string of SQL, meaning one thing on one engine and failing to parse on another.
});

// Whole-number attribute — A whole-number attribute carries a signed 32-bit integer on a declaration.
test.skip("metaobjects.declare.attributeValueTypes.attrInt", () => {
  // unfilled slot — write an assertion that fails when: A length limit written as text, so a limit of 128 compares as smaller than a limit of 64.
});

// Symbol-to-integer map — A symbol-to-integer map attribute carries an explicit mapping from declared symbols to numbers, so the correspondence survives the symbols being reordered.
test.skip("metaobjects.declare.attributeValueTypes.attrIntMap", () => {
  // unfilled slot — write an assertion that fails when: A set of members mapped to storage by position, where inserting one member silently re-labels every row already written.
});

// Large whole-number attribute — A large whole-number attribute carries a signed 64-bit integer on a declaration.
test.skip("metaobjects.declare.attributeValueTypes.attrLong", () => {
  // unfilled slot — write an assertion that fails when: A bound above the 32-bit range quietly narrowed at load, so the declaration means less than it says.
});

// Free-form properties — A free-form properties attribute carries author- supplied key-value data that the metamodel deliberately does not interpret, so a project can record something of its own without inventing vocabulary.
test.skip("metaobjects.declare.attributeValueTypes.attrProperties", () => {
  // unfilled slot — write an assertion that fails when: A project needing one note of its own on a declaration and having to choose between an attribute the loader refuses and switching the refusal off.
});

// Text attribute — A text attribute carries characters on a declaration and is checked as text when the model loads.
test.skip("metaobjects.declare.attributeValueTypes.attrString", () => {
  // unfilled slot — write an assertion that fails when: A number written where a name belongs, coerced to text, and emitted into generated code as a name.
});

// Element count — An element-count rule bounds how many entries a repeated value may hold.
test.skip("metaobjects.declare.constraints.validatorArray", () => {
  // unfilled slot — write an assertion that fails when: An unbounded list accepted into a field whose reader only ever looks at the first few.
});

// At least one — An at-least-one rule requires that some member of a declared group is given.
test.skip("metaobjects.declare.constraints.validatorAtLeastOne", () => {
  // unfilled slot — write an assertion that fails when: A contact saved with neither an email address nor a phone number, and no way to reach them.
});

// Comparison — A comparison rule relates one declared value to another on the same object.
test.skip("metaobjects.declare.constraints.validatorComparison", () => {
  // unfilled slot — write an assertion that fails when: An end date before its start date, stored, and every report that spans them silently empty.
});

// Length bounds — A length rule bounds how long a value may be, and an explicitly declared bound is authoritative over any implicit floor.
test.skip("metaobjects.declare.constraints.validatorLength", () => {
  // unfilled slot — write an assertion that fails when: A declared minimum of zero raised to one behind the author's back, so provided-but-empty becomes inexpressible.
});

// Minimum length — A declared minimum is authoritative and is never widened by an implicit floor, so zero means provided-and-possibly-empty and is able to say so.
test.skip("metaobjects.declare.constraints.validatorLength.min", () => {
  // unfilled slot — write an assertion that fails when: A minimum of zero raised to one behind the author's back, making required-but-empty inexpressible.
});

// Numeric bounds — A numeric rule bounds the range a number may take.
test.skip("metaobjects.declare.constraints.validatorNumeric", () => {
  // unfilled slot — write an assertion that fails when: A negative quantity accepted, so an order adds to stock instead of taking from it.
});

// Present together — A present-together rule ties two values so that either both are given or neither is.
test.skip("metaobjects.declare.constraints.validatorPresentIff", () => {
  // unfilled slot — write an assertion that fails when: A latitude stored with no longitude, and a point that cannot be placed.
});

// Pattern — A pattern rule constrains a value to a declared expression, matched in FULL rather than anywhere within it.
test.skip("metaobjects.declare.constraints.validatorRegex", () => {
  // unfilled slot — write an assertion that fails when: A pattern meant to accept a postcode accepting any text that contains one.
});

// Required — A required rule says a value must be provided, and for text that emptiness is not providing it.
test.skip("metaobjects.declare.constraints.validatorRequired", () => {
  // unfilled slot — write an assertion that fails when: An empty string accepted for a name, so a record exists with nobody's name on it.
});

// Conditionally required — A conditional-requirement rule makes a value mandatory only in a declared circumstance.
test.skip("metaobjects.declare.constraints.validatorRequiredWhen", () => {
  // unfilled slot — write an assertion that fails when: A cancellation reason demanded on every order, or on none of them.
});

// Output template — An output template declares a rendering that only ever goes outward. Its axis is DIRECTION, so it has no inbound tier and emits no parser.
test.skip("metaobjects.declare.declaredPrompts.templateOutput", () => {
  // unfilled slot — write an assertion that fails when: An outbound-only template given a parser for a response it will never receive.
});

// Prompt template — A prompt template declares an outbound message to a model: the payload it carries, and — when it expects a structured reply — the shape that reply must take.
test.skip("metaobjects.declare.declaredPrompts.templatePrompt", () => {
  // unfilled slot — write an assertion that fails when: A prompt whose payload is assembled somewhere else, so nothing can tell whether a renamed field still reaches it.
});

// Declared payload — A payload reference names the declared shape a prompt sends, which is what makes what-it- sends a fact about the model and payload growth a diff.
test.skip("metaobjects.declare.declaredPrompts.templatePrompt.payloadRef", () => {
  // unfilled slot — write an assertion that fails when: A prompt assembling its payload from whatever the caller passed, so nobody can say what it sends without running it.
});

// Declared response — A response reference is what makes a prompt a RESPONDING prompt: it declares the shape a reply must take, and the parser, the tolerant extractor and the response-format fragment all key off it.
test.skip("metaobjects.declare.declaredPrompts.templatePrompt.responseRef", () => {
  // unfilled slot — write an assertion that fails when: A reply parsed against a shape the prompt never asked for.
});

// External prompt text — A text reference keeps a prompt's words outside the model and resolved by the provider, so changing what a prompt SAYS is not a change to the model.
test.skip("metaobjects.declare.declaredPrompts.templatePrompt.textRef", () => {
  // unfilled slot — write an assertion that fails when: Prompt wording embedded in the declaration, making every rewording a metadata change and a regeneration.
});

// Tool call template — A tool-call template declares a tool a model may invoke by name, with a declared payload and a declared budget for the call.
test.skip("metaobjects.declare.declaredPrompts.templateToolcall", () => {
  // unfilled slot — write an assertion that fails when: A tool a model can call whose arguments nothing declares, so a wrong shape is discovered by the tool itself.
});

// Aggregate — An aggregate declares a value computed over related rows, optionally scoped, and derived rather than stored.
test.skip("metaobjects.declare.derivation.originAggregate", () => {
  // unfilled slot — write an assertion that fails when: A count kept in a column and maintained by hand, disagreeing with the rows it counts.
});

// Aggregation — The aggregation says what is computed over the related rows — a count, a sum, an extreme, a whole-row rollup — and the result is derived, never stored.
test.skip("metaobjects.declare.derivation.originAggregate.agg", () => {
  // unfilled slot — write an assertion that fails when: A total maintained by application code, drifting from the rows it totals with nothing to notice.
});

// Aggregate scope — A scoping filter narrows which related rows an aggregate counts, as declared structure rather than as a fragment of query language.
test.skip("metaobjects.declare.derivation.originAggregate.filter", () => {
  // unfilled slot — write an assertion that fails when: An open-orders count that includes the closed ones, because the scoping lived in a hand-written view nobody regenerated.
});

// What is aggregated — The of attribute names what is aggregated; omitting it on a collecting aggregate means each related row is collected AS the carrying field's declared value object.
test.skip("metaobjects.declare.derivation.originAggregate.of", () => {
  // unfilled slot — write an assertion that fails when: A rollup returning the whole entity where the declaration asked for a curated shape, because the target was inferred instead of read.
});

// Path to the rows — The via attribute names the path from the base object to what is aggregated, and may be left out only when exactly one single-hop relationship leads there.
test.skip("metaobjects.declare.derivation.originAggregate.via", () => {
  // unfilled slot — write an assertion that fails when: Two relationships reaching the same object and the aggregate silently picking one of them.
});

// Computed value — A computed value declares a value produced by a declared expression over the row it sits on.
test.skip("metaobjects.declare.derivation.originComputed", () => {
  // unfilled slot — write an assertion that fails when: A derived figure written directly, so it stops matching the inputs it was derived from.
});

// First of many — A first-of-many declares a value taken from one related row, chosen by a declared order rather than by chance.
test.skip("metaobjects.declare.derivation.originFirst", () => {
  // unfilled slot — write an assertion that fails when: A latest value that is whichever row the database happened to return.
});

// Passthrough — A passthrough declares that a value IS a value on another object, and must keep its type across the hop.
test.skip("metaobjects.declare.derivation.originPassthrough", () => {
  // unfilled slot — write an assertion that fails when: A number carried through as text because the two ends disagreed about its type, with nothing saying so.
});

// Deliberate type change — The convert flag is the explicit acknowledgement that a passthrough changes type. Without it a mismatch across the hop fails the load rather than being coerced.
test.skip("metaobjects.declare.derivation.originPassthrough.convert", () => {
  // unfilled slot — write an assertion that fails when: A number carried through into a text field with nothing recording that anyone meant it to convert.
});

// Source of the value — The from attribute names the value this one IS, on the object it comes from.
test.skip("metaobjects.declare.derivation.originPassthrough.from", () => {
  // unfilled slot — write an assertion that fails when: A field that looks like a copy and is a re- implementation, so the two drift apart and nothing compares them.
});

// Architectural requirement — An architectural requirement states a policy that must hold UNIVERSALLY, and its check is universality: it fails when something is outside it.
test.skip("metaobjects.declare.governanceVocabulary.requirementArchitectural", () => {
  // unfilled slot — write an assertion that fails when: A rule declared once, applied to nothing, with every gate reporting green.
});

// Functional requirement — A functional requirement states a capability that must EXIST, and its check is existence: it fails when nothing implements it.
test.skip("metaobjects.declare.governanceVocabulary.requirementFunctional", () => {
  // unfilled slot — write an assertion that fails when: A capability everyone believes is there, that nothing builds and no check misses.
});

// Link to the model — The implementedBy attribute names the model nodes realising a requirement, and is legal only where a requirement addresses the model at all — at an object or one of its members, never at an organisational tier.
test.skip("metaobjects.declare.governanceVocabulary.requirementFunctional.implementedBy", () => {
  // unfilled slot — write an assertion that fails when: A solution-level entry holding a list of every entity, so the tier that exists to delegate is also the tier that holds every link.
});

// Model document — A model document is what declarations are written in. It names the package they belong to, which is what lets a bare reference bind locally and a qualified one bind exactly.
test.skip("metaobjects.declare.inheritanceAndReferences.metadataRoot", () => {
  // unfilled slot — write an assertion that fails when: One name declared in two packages and a reference to it binding to whichever was read first.
});

// Entity — An entity owns its data: it has its own identity, a writable place for that data to live, and a lifecycle.
test.skip("metaobjects.declare.objectTaxonomy.objectEntity", () => {
  // unfilled slot — write an assertion that fails when: A record that cannot be updated, because nothing declares where it lives or which values identify one of it.
});

// Projection — A projection is a derived read-only representation whose declared field set IS its exposure: an inclusive list that fails closed.
test.skip("metaobjects.declare.objectTaxonomy.objectProjection", () => {
  // unfilled slot — write an assertion that fails when: A view that starts returning a column nobody listed, because the table underneath it gained one.
});

// Row scope — A projection's row filter declares which rows the projection exposes at all, as part of the projection rather than as something every caller must remember to add.
test.skip("metaobjects.declare.objectTaxonomy.objectProjection.filter", () => {
  // unfilled slot — write an assertion that fails when: A view meant to show active records showing every record, because scoping was left to each query.
});

// Value object — A value object is pure shape — never its own identity, never its own source — constructed by whoever embeds it rather than fetched.
test.skip("metaobjects.declare.objectTaxonomy.objectValue", () => {
  // unfilled slot — write an assertion that fails when: A transport shape that has acquired a primary key, so two copies of one message are now two records.
});

// Primary key — A primary key declares which values identify one row and how a new row gets its identifier.
test.skip("metaobjects.declare.persistenceMapping.identityPrimary", () => {
  // unfilled slot — write an assertion that fails when: Two rows nothing can tell apart, because nothing declared what makes one row one row.
});

// Key generation — The generation attribute says where a new row's key comes from — the database, the writer, or nowhere, which means the caller must supply it.
test.skip("metaobjects.declare.persistenceMapping.identityPrimary.generation", () => {
  // unfilled slot — write an assertion that fails when: A key the caller must supply, treated as optional by the create shape, so the insert reaches the database with nothing in it.
});

// Reference — A reference declares that a value points at another object's key, and what becomes of it when that object goes.
test.skip("metaobjects.declare.persistenceMapping.identityReference", () => {
  // unfilled slot — write an assertion that fails when: A parent deleted and its children left pointing at nothing, because the reference declared no action.
});

// Constraint or navigation — Enforcement declares whether a reference is a database constraint or a navigational fact, so a shape can point at an entity without acquiring a foreign key.
test.skip("metaobjects.declare.persistenceMapping.identityReference.enforce", () => {
  // unfilled slot — write an assertion that fails when: A message referring to an order by its number, given a foreign key, so the message cannot be stored before the order it describes.
});

// Delete action — The delete action declares what becomes of a referencing row when what it references goes, and an unconfigured reference does nothing rather than whatever the framework underneath would have chosen.
test.skip("metaobjects.declare.persistenceMapping.identityReference.onDelete", () => {
  // unfilled slot — write an assertion that fails when: A parent deleted and its children silently deleted with it, because a framework supplied a cascade nobody declared.
});

// Reference target — A reference names the object and key it points at, and that target is resolved when the model loads rather than when a query runs.
test.skip("metaobjects.declare.persistenceMapping.identityReference.references", () => {
  // unfilled slot — write an assertion that fails when: A foreign key pointing at something renamed months ago, discovered at the first insert.
});

// Alternate key — An alternate key declares a second unique way to address a row; its uniqueness comes from being that type, never from an attribute somebody remembered to set.
test.skip("metaobjects.declare.persistenceMapping.identitySecondary", () => {
  // unfilled slot — write an assertion that fails when: Two customers sharing an email address, because the index meant to prevent it was never unique.
});

// Lookup index — A lookup index declares a non-unique retrieval path. Uniqueness lives in the TYPE, so an index cannot claim it by accident.
test.skip("metaobjects.declare.persistenceMapping.indexLookup", () => {
  // unfilled slot — write an assertion that fails when: A retrieval index that silently enforces uniqueness, so the second row with the same value is refused.
});

// Expression key — An expression index declares that what is indexed is computed rather than stored, and it is EXCLUSIVE with naming fields — one key or the other, never a silent half.
test.skip("metaobjects.declare.persistenceMapping.indexLookup.expr", () => {
  // unfilled slot — write an assertion that fails when: An index declaring both a field list and an expression, where half of it is quietly discarded and the index built is not the index described.
});

// Relational source — A relational source says where an object's data lives, in what kind of place, and in what role, without naming a database engine.
test.skip("metaobjects.declare.persistenceMapping.sourceRdb", () => {
  // unfilled slot — write an assertion that fails when: A model that only reveals which engine it assumed when a migration fails against a different one.
});

// Kind of place — The kind attribute says what sort of place data lives in, and read-only-ness is DERIVED from it rather than declared a second time alongside it.
test.skip("metaobjects.declare.persistenceMapping.sourceRdb.kind", () => {
  // unfilled slot — write an assertion that fails when: A view marked writable, so generated code offers an update the database will refuse.
});

// Read or write role — The role attribute separates the one place an object is written from the places it is read, and exactly one source is the primary.
test.skip("metaobjects.declare.persistenceMapping.sourceRdb.role", () => {
  // unfilled slot — write an assertion that fails when: Two sources both claiming to be where writes go, and a row landing in whichever the code reached first.
});

// Hand-written body — A hand-written body declares SQL the model cannot express, and taking that escape hatch means owning the result: nothing derives it and nothing checks it back against the model.
test.skip("metaobjects.declare.persistenceMapping.sourceRdb.sql", () => {
  // unfilled slot — write an assertion that fails when: A hand-written view whose columns no longer match the projection naming it, with every generated artifact still built from the projection.
});

// Not ours to change — The unmanaged marker declares that a database object is not the toolchain's to create or alter, so a migration proposes nothing for it.
test.skip("metaobjects.declare.persistenceMapping.sourceRdb.unmanaged", () => {
  // unfilled slot — write an assertion that fails when: A hand-maintained view rewritten by a migration that assumed it owned it.
});

// Data grid — A data grid declares which of an object's fields a tabular view shows, in what order, and how it is sorted and paged before anyone touches it.
test.skip("metaobjects.declare.presentation.layoutDataGrid", () => {
  // unfilled slot — write an assertion that fails when: A table that gains a column every time the model does, including the ones nobody meant to show.
});

// Shown columns — The column list declares what a tabular view shows and in what order, INCLUSIVELY — so a field added to the model does not appear in a grid by having been added.
test.skip("metaobjects.declare.presentation.layoutDataGrid.columns", () => {
  // unfilled slot — write an assertion that fails when: An internal field appearing in a customer- facing table the day it was added to the model.
});

// Checkbox — A checkbox declares that a boolean is set by a single on-or-off control.
test.skip("metaobjects.declare.presentation.viewCheckbox", () => {
  // unfilled slot — write an assertion that fails when: A yes-or-no answer offered as a text box, so y, yes and true all arrive and all mean the same thing.
});

// Money display — A money display declares that an amount is shown in the conventions of a declared locale, formatted where it is read and never on the server.
test.skip("metaobjects.declare.presentation.viewCurrency", () => {
  // unfilled slot — write an assertion that fails when: An amount formatted once on the server for one locale and shown to a reader in another.
});

// Formatting locale — The locale says which conventions an amount is formatted in, and formatting happens where the value is read rather than where it is stored.
test.skip("metaobjects.declare.presentation.viewCurrency.locale", () => {
  // unfilled slot — write an assertion that fails when: An amount formatted once on the server for one reader's conventions and shown to another's.
});

// Date picker — A date control declares that a value is chosen from a calendar.
test.skip("metaobjects.declare.presentation.viewDate", () => {
  // unfilled slot — write an assertion that fails when: A date typed as free text, so every reader invents their own format and half of them are ambiguous.
});

// Dropdown — A dropdown declares that a value is chosen from its declared members in a collapsed list.
test.skip("metaobjects.declare.presentation.viewDropdown", () => {
  // unfilled slot — write an assertion that fails when: A closed set of members offered as free text, so a typo silently becomes a new value.
});

// Hidden value — A hidden control declares that a value travels with a form without being shown or edited.
test.skip("metaobjects.declare.presentation.viewHidden", () => {
  // unfilled slot — write an assertion that fails when: A value the form has to carry, re-fetched on submit because there was no way to carry it.
});

// Link — A link control declares that a value is shown as a link to whatever it addresses.
test.skip("metaobjects.declare.presentation.viewHotlink", () => {
  // unfilled slot — write an assertion that fails when: An address displayed as text nobody can follow without selecting and copying it.
});

// Image upload — An image control declares that a value is an image a person uploads and crops, stored as an opaque key so no image bytes ever cross the model.
test.skip("metaobjects.declare.presentation.viewImage", () => {
  // unfilled slot — write an assertion that fails when: A record carrying its own image bytes, so every list of records carries every picture in it.
});

// Month picker — A month control declares that a value is chosen as a month within a year, with no day.
test.skip("metaobjects.declare.presentation.viewMonth", () => {
  // unfilled slot — write an assertion that fails when: A billing period entered as a full date, so twelve possible values become three hundred and sixty- five.
});

// Number input — A number control declares that a value is entered as a number rather than as text that must later parse.
test.skip("metaobjects.declare.presentation.viewNumber", () => {
  // unfilled slot — write an assertion that fails when: A quantity typed with a thousands separator and refused by the server instead of by the field.
});

// Masked input — A masked control declares that a value is entered without being displayed.
test.skip("metaobjects.declare.presentation.viewPassword", () => {
  // unfilled slot — write an assertion that fails when: A secret typed in plain sight and left legible in a screen recording.
});

// Radio group — A radio group declares that a value is chosen from its declared members with every option visible at once.
test.skip("metaobjects.declare.presentation.viewRadio", () => {
  // unfilled slot — write an assertion that fails when: A three-way choice hidden behind a control that shows one option until someone opens it.
});

// Single-line text — A single-line text control declares that a value is edited as one line.
test.skip("metaobjects.declare.presentation.viewText", () => {
  // unfilled slot — write an assertion that fails when: A short label given a box the height of a paragraph, because the interface had only the type to guess from.
});

// Multi-line text — A multi-line text control declares that a value is edited across several lines, to a declared visible height.
test.skip("metaobjects.declare.presentation.viewTextarea", () => {
  // unfilled slot — write an assertion that fails when: A paragraph edited through a one-line control, so the author cannot see what they have written.
});

// Web-rendered view base — An abstract base groups the views that render in a browser, so what they share is declared once rather than on each of them.
test.skip("metaobjects.declare.presentation.viewWeb", () => {
  // unfilled slot — write an assertion that fails when: A property common to every browser-rendered view restated on each one, where adding a view means remembering to restate it again.
});

// Aggregation — An aggregation declares that one object groups others which outlive it.
test.skip("metaobjects.declare.relationships.relationshipAggregation", () => {
  // unfilled slot — write an assertion that fails when: A team disbanded and its members deleted along with it.
});

// Association — An association declares that two objects refer to one another, with neither owning the other's lifetime.
test.skip("metaobjects.declare.relationships.relationshipAssociation", () => {
  // unfilled slot — write an assertion that fails when: Deleting a category taking every product in it, because a reference was modelled as ownership.
});

// Cardinality — Cardinality says whether one side reaches one row or many, and it is what decides the shape of everything generated from the relationship.
test.skip("metaobjects.declare.relationships.relationshipAssociation.cardinality", () => {
  // unfilled slot — write an assertion that fails when: A one-to-many read as one-to-one, so a customer's second order is unreachable.
});

// Junction entity — A through-entity names the junction a many- to-many passes over, and the relationship's keys are DERIVED from that junction's own references rather than restated here.
test.skip("metaobjects.declare.relationships.relationshipAssociation.through", () => {
  // unfilled slot — write an assertion that fails when: A join table's key direction stated twice — once on the junction, once on the relationship — and the two disagreeing.
});

// Composition — A composition declares that one object owns another's lifetime, so the parent going takes the children with it.
test.skip("metaobjects.declare.relationships.relationshipComposition", () => {
  // unfilled slot — write an assertion that fails when: An order deleted and its lines left behind, referenced by nothing and still counted by every report.
});

// Boolean — A boolean field holds exactly true or false, and never a third state standing in for absent.
test.skip("metaobjects.declare.typedFieldVocabulary.fieldBoolean", () => {
  // unfilled slot — write an assertion that fails when: An unanswered question stored as false, so nobody can tell it apart from an answer of no.
});

// Money — A money field holds an amount as a whole number of the minor units of a declared currency, and is never handled as a fraction.
test.skip("metaobjects.declare.typedFieldVocabulary.fieldCurrency", () => {
  // unfilled slot — write an assertion that fails when: A price held as a binary fraction, where adding a hundred of them is off by a cent.
});

// Currency code — The currency attribute names which currency an amount is in, and fixes what one minor unit of it means for that field.
test.skip("metaobjects.declare.typedFieldVocabulary.fieldCurrency.currency", () => {
  // unfilled slot — write an assertion that fails when: A yen amount divided by a hundred because whoever read it assumed two decimal places.
});

// Calendar date — A date field holds a day on a calendar — no time, no zone.
test.skip("metaobjects.declare.typedFieldVocabulary.fieldDate", () => {
  // unfilled slot — write an assertion that fails when: A birthday that lands on the previous day for a reader in another region.
});

// Exact decimal — An exact-decimal field holds a base-10 number of declared precision and scale, and never passes through binary floating point to get there.
test.skip("metaobjects.declare.typedFieldVocabulary.fieldDecimal", () => {
  // unfilled slot — write an assertion that fails when: A tax figure that differs by a cent depending on which language computed it.
});

// Double-precision number — A double-precision field holds a binary floating- point number, with the inexactness that implies stated rather than discovered.
test.skip("metaobjects.declare.typedFieldVocabulary.fieldDouble", () => {
  // unfilled slot — write an assertion that fails when: A running total accumulated in one, where the sum of the parts no longer equals the whole.
});

// Enumeration — An enumeration field holds one member of a closed declared set, and the member symbol is what the wire and the runtime see whatever the column stores.
test.skip("metaobjects.declare.typedFieldVocabulary.fieldEnum", () => {
  // unfilled slot — write an assertion that fails when: A stored value that is no declared member, handed back as though it were one.
});

// Explicit integer coding — An integer coding fixes each member's stored number EXPLICITLY, so reordering the members cannot re-label rows already written.
test.skip("metaobjects.declare.typedFieldVocabulary.fieldEnum.intValueMap", () => {
  // unfilled slot — write an assertion that fails when: A member inserted into the middle of the list, and every stored row now meaning the member after the one it meant yesterday.
});

// Incoming-value normalisation — Normalisation says how loosely an incoming spelling may match a member before it is accepted.
test.skip("metaobjects.declare.typedFieldVocabulary.fieldEnum.normalize", () => {
  // unfilled slot — write an assertion that fails when: Two members whose symbols differ only by a separator, where a delimited value silently matches the wrong one and is reported as extracted rather than malformed.
});

// Member set — The member list IS the legal set, and nothing outside it is a legal value at any tier.
test.skip("metaobjects.declare.typedFieldVocabulary.fieldEnum.values", () => {
  // unfilled slot — write an assertion that fails when: A member added to a database constraint and not to the model, so one path accepts it and every other refuses it.
});

// Single-precision number — A single-precision field holds a narrower binary floating-point number, for values whose provenance is already single-precision.
test.skip("metaobjects.declare.typedFieldVocabulary.fieldFloat", () => {
  // unfilled slot — write an assertion that fails when: A value stored at one precision and read back at another, reported as changed by a check that compared the two.
});

// IP address — An address field holds an IP address literal, and a name is never resolved into one on its behalf.
test.skip("metaobjects.declare.typedFieldVocabulary.fieldInet", () => {
  // unfilled slot — write an assertion that fails when: A hostname turned into an address by a lookup while the request was being served, so what got stored depends on when it was stored.
});

// Strictness opt-out — Leniency on an address field opts out of requiring a literal AND changes the column it is stored in, so it is a schema decision rather than a validation preference.
test.skip("metaobjects.declare.typedFieldVocabulary.fieldInet.lenient", () => {
  // unfilled slot — write an assertion that fails when: A field relaxed to accept anything, still typed as an address underneath, refusing the very values it was relaxed for.
});

// Whole number — A whole-number field holds a signed 32-bit integer, in every port and on the wire.
test.skip("metaobjects.declare.typedFieldVocabulary.fieldInt", () => {
  // unfilled slot — write an assertion that fails when: A count that wraps to a negative number on one port and not on another.
});

// Large whole number — A large whole-number field holds a signed 64-bit integer and crosses the wire without a floating- point conversion in between.
test.skip("metaobjects.declare.typedFieldVocabulary.fieldLong", () => {
  // unfilled slot — write an assertion that fails when: An identifier above two to the fifty-third arriving in a browser rounded to a different identifier.
});

// Map — A map field holds key-to-value pairs whose keys are not known when the model is written.
test.skip("metaobjects.declare.typedFieldVocabulary.fieldMap", () => {
  // unfilled slot — write an assertion that fails when: An open-ended set of properties modelled as one column per key anyone thought of, so adding a key is a migration.
});

// Embedded object — An object field holds a declared value object, stored flattened, as a JSON document, or as a subdocument, according to the declaration.
test.skip("metaobjects.declare.typedFieldVocabulary.fieldObject", () => {
  // unfilled slot — write an assertion that fails when: A structured value kept as opaque text, so nothing can query inside it and nothing checks its shape.
});

// Value-object target — An object reference names the value object a field holds, and that target must be a shape — never something carrying its own identity.
test.skip("metaobjects.declare.typedFieldVocabulary.fieldObject.objectRef", () => {
  // unfilled slot — write an assertion that fails when: A field declaring it holds a shape, pointed at an entity, and generated code embedding a whole table's row inside another.
});

// Physical storage form — The storage attribute says how an embedded object is physically kept — spread across columns, as one queryable document, or as a nested record.
test.skip("metaobjects.declare.typedFieldVocabulary.fieldObject.storage", () => {
  // unfilled slot — write an assertion that fails when: A nested value spread into columns by one port and kept as a document by another, so neither can read the other's rows.
});

// Text — A text field holds characters up to a declared maximum length, and attaches no meaning to what they spell beyond what a validator adds.
test.skip("metaobjects.declare.typedFieldVocabulary.fieldString", () => {
  // unfilled slot — write an assertion that fails when: Text accepted at a length nothing downstream can store, found when the insert fails rather than when it was typed.
});

// Maximum length — A maximum length bounds a text value everywhere it is checked, and where a separate length rule also applies, the stricter of the two wins.
test.skip("metaobjects.declare.typedFieldVocabulary.fieldString.maxLength", () => {
  // unfilled slot — write an assertion that fails when: One limit on the field and a longer one as a rule, with generated code enforcing whichever it happened to read last.
});

// Recognised text format — A string format constrains text that has a recognised shape but no behaviour of its own — the residue left once everything with a native type became its own field type.
test.skip("metaobjects.declare.typedFieldVocabulary.fieldString.stringFormat", () => {
  // unfilled slot — write an assertion that fails when: A value with real behaviour validated as a format string, so nothing downstream knows what it is.
});

// Time of day — A time field holds a time of day with no date attached to it.
test.skip("metaobjects.declare.typedFieldVocabulary.fieldTime", () => {
  // unfilled slot — write an assertion that fails when: An opening hour that cannot be declared without inventing a date to hang it on.
});

// Instant — A timestamp field holds an instant, unambiguous across zones, and holds a naive local reading only where the declaration says so.
test.skip("metaobjects.declare.typedFieldVocabulary.fieldTimestamp", () => {
  // unfilled slot — write an assertion that fails when: Two events whose recorded order changes when a server moves region.
});

// Server-stamped — An auto-set timestamp is stamped by the tier that writes it, on create or on update as declared, and a value the caller supplies is ignored rather than honoured.
test.skip("metaobjects.declare.typedFieldVocabulary.fieldTimestamp.autoSet", () => {
  // unfilled slot — write an assertion that fails when: A caller setting a record's creation time to whatever suits them, so the audit trail records what the client claimed.
});

// Naive local-time opt-out — The local-time flag is the opt-out from instant semantics: absent, the value is an instant; present, it is a wall-clock reading carrying no zone.
test.skip("metaobjects.declare.typedFieldVocabulary.fieldTimestamp.localTime", () => {
  // unfilled slot — write an assertion that fails when: A wall-clock opening time shifted by an hour, because it was stored as an instant in a zone nobody declared.
});

// URI — A URI field holds an absolute, scheme-bearing address, unless the declaration explicitly relaxes that.
test.skip("metaobjects.declare.typedFieldVocabulary.fieldUri", () => {
  // unfilled slot — write an assertion that fails when: A relative path accepted into a field that everything downstream will try to dereference.
});

// Strictness opt-out — Leniency is the declared opt-out from requiring an absolute, scheme-bearing address. Without it strictness is not a per- port judgement call.
test.skip("metaobjects.declare.typedFieldVocabulary.fieldUri.lenient", () => {
  // unfilled slot — write an assertion that fails when: One port accepting a relative address and another refusing it, from one declaration.
});

// Unique identifier — A unique-identifier field holds a UUID as a value in its own right, not as text that happens to be shaped like one.
test.skip("metaobjects.declare.typedFieldVocabulary.fieldUuid", () => {
  // unfilled slot — write an assertion that fails when: Two rows differing only in the letter case of their identifier, treated as two different rows.
});
