# Migration — commit `.gen-state/.hashes.json`

**Applies to:** every existing TypeScript project scaffolded by `meta init` before this
release. **Action required:** one edit to `.metaobjects/.gitignore`, then commit one file.

## What changed, and why it matters to you

`meta gen` used to overwrite a generated file whose content differed from fresh output
whenever it had no merge-base snapshot for it — silently, and reported as `NEW`. Because
`meta init` gitignored the whole of `.gen-state/`, that was the state of **every fresh
clone and every CI runner**, which is precisely where the promise that hand edits survive
regeneration mattered most.

It now refuses instead, naming the file. Making that possible needs one small artifact to
travel with your repository.

## The edit

In `.metaobjects/.gitignore`, replace:

```gitignore
.gen-state/
```

with:

```gitignore
.gen-state/*
!.gen-state/.hashes.json
```

**The glob is load-bearing.** `.gen-state/` names a *directory*, and git does not descend
into an excluded directory, so the negation below it can never take effect — the manifest
would stay ignored and you would get none of this. `.gen-state/*` excludes the directory's
*contents*, which leaves the negation able to re-include one of them.

Then commit the manifest:

```bash
meta gen                                  # writes .gen-state/.hashes.json
git add .metaobjects/.gitignore .metaobjects/.gen-state/.hashes.json
git commit -m "chore: commit the codegen hash manifest"
```

## Why the manifest and not the whole snapshot

`.gen-state/` holds two different things. The snapshot **bodies** are a full second copy of
all your generated output — committing those doubles the generated bytes in your repository
and puts a second diff beside every real one. `.hashes.json` is one line per generated path:

```json
{
  "src/generated/Author.ts": "9f2b...",
  "src/generated/Post.ts": "41ce..."
}
```

That is enough to answer the only question the decision needs — *is this file byte-for-byte
what I wrote?* — so it is what gets committed. Keys are sorted, so the diff is stable and
two people who both regenerated do not conflict over ordering.

## What you will see afterwards

| situation | before | after |
|---|---|---|
| File matches its recorded hash (a formatter or engine bump moved the output) | overwritten, reported `NEW` | overwritten, reported `overwrite` |
| File carries a hand edit | **overwritten silently** | `refused`, path named, exit code 1 |
| File we have no record of writing | **overwritten silently** | `refused`, path named, exit code 1 |
| Snapshot body present (the machine that generated it) | three-way merge | three-way merge, unchanged |

## If your first run after migrating refuses a lot of files

That means those files differ from fresh output and there is no hash recording them yet —
`meta gen` genuinely cannot tell an intentional edit from stale output. Two ways forward:

- **Keep the differences.** Move each edit into a non-generated file (a
  `<Entity>.extra.ts` sibling is the usual place) and re-run.
- **Discard them.** `meta gen --baseline=fresh` overwrites and adopts fresh output as the
  baseline. This throws away hand edits in generated files, so read the refusal list first.

Run it once, commit the manifest, and the refusals stop: from then on every machine can
tell your edits from its own output.

## If you deliberately do not commit generated output

Nothing changes for you. Output that is not committed does not exist on a fresh clone, so
`meta gen` writes it as new. The refusal only ever fires on a file that is already there.
