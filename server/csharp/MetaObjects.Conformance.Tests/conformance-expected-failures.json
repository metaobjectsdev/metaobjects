{
  "language": "csharp",
  "_comment": "FR5a envelope-shape gates: TS is the reference port for source.format / source.files / source.jsonPath. C# is now reconciled with TS on the JSON-side FR5a envelopes (parser malformed-JSON catch builds a $-rooted JsonSource with the source-file name; inline-attr loop reports ERR_RESERVED_ATTR / ERR_UNKNOWN_ATTR / ERR_BAD_ATTR_VALUE at the parent's path — not key-deeper). Keep this file as the documented ledger surface.",
  "_notes": "FR5d (2026-05-27) — C# is ahead of fixtures: the five reference-resolution sites (extends, @payloadRef, @requiredSlots, @via, @of/@from) now emit format=resolved with referrer + target, mirroring the TS reference (commit f505681e). The corresponding corpus fixtures still expect FR5a's format=json envelope; they will be migrated to format=resolved in the cross-port FR5d-finalize commit after all four ports land. Each ledger entry below pairs with the same TS interim entry in server/typescript/packages/metadata/conformance-expected-failures.json.",
  "fixtures": [
    "error-extends-nonexistent",
    "error-origin-bad-via-path",
    "error-template-payload-ref-not-value",
    "error-template-payload-ref-unresolved",
    "error-template-required-slot-missing"
  ]
}
