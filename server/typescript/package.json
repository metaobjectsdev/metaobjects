{
  "name": "@metaobjectsdev/monorepo",
  "version": "0.5.0",
  "private": true,
  "description": "TypeScript implementation of the MetaObjects standard",
  "type": "module",
  "scripts": {
    "build": "bun run --filter '*' build",
    "test": "bun test",
    "typecheck": "bun run --filter '*' typecheck",
    "clean": "rm -rf packages/*/dist packages/*/*.tsbuildinfo"
  },
  "workspaces": ["packages/*", "../../client/web/packages/*"],
  "devDependencies": {
    "typescript": "^5.6.0"
  },
  "engines": { "bun": ">=1.3.0", "node": ">=22.0.0" },
  "packageManager": "bun@1.3.8"
}
