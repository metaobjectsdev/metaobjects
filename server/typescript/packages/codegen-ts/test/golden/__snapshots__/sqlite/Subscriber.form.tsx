// @generated by @metaobjectsdev/codegen-ts — DO NOT EDIT.
// Source metadata: Subscriber (Subscriber)
// Customize via Subscriber.extra.tsx (custom layouts, per-field components, etc.).
import { Subscriber, SubscriberInsertSchema } from "./Subscriber";
import type { Subscriber as SubscriberRow } from "./Subscriber";
import { useEntityForm } from "@metaobjectsdev/react";
import type { ReactElement } from "react";
import type { SubmitHandler } from "react-hook-form";

export interface SubscriberFormProps {
  onSubmit: SubmitHandler<Partial<SubscriberRow>>;
  defaultValues?: Partial<SubscriberRow>;
  submitLabel?: string;
  className?: string;
}

/**
 * Generated React form for Subscriber. All field metadata (name, label,
 * placeholder, html input type, RHF validation rules) comes from the
 * Subscriber constants emitted alongside this file. Validation uses the
 * generated Zod schema, shared with the server-side Fastify route.
 *
 * Customize: this file is regenerated on every `forge gen`. If you need a
 * custom layout, write your own component using useEntityForm + the
 * Subscriber constants directly.
 */
export function SubscriberForm(props: SubscriberFormProps): ReactElement {
  const form = useEntityForm(
    Subscriber,
    SubscriberInsertSchema,
    props.defaultValues !== undefined ? { defaultValues: props.defaultValues } : {},
  );
  return (
    <form
      className={props.className ?? "metaobjects-form"}
      data-entity={Subscriber.$entity}
      onSubmit={form.handleSubmit(props.onSubmit as never)}
    >
      <div className="metaobjects-field" key="email">
        <label className="metaobjects-field-label" htmlFor={Subscriber.email.name}>{Subscriber.email.label}</label>
        <input className="metaobjects-field-input" {...form.input.email} />
        {form.formState.errors.email !== undefined && (
          <span className="metaobjects-field-error" role="alert">
            {String(form.formState.errors.email?.message ?? "")}
          </span>
        )}
      </div>
      <div className="metaobjects-field" key="firstName">
        <label className="metaobjects-field-label" htmlFor={Subscriber.firstName.name}>
          {Subscriber.firstName.label}
        </label>
        <input className="metaobjects-field-input" {...form.input.firstName} />
        {form.formState.errors.firstName !== undefined && (
          <span className="metaobjects-field-error" role="alert">
            {String(form.formState.errors.firstName?.message ?? "")}
          </span>
        )}
      </div>
      <div className="metaobjects-field" key="lastName">
        <label className="metaobjects-field-label" htmlFor={Subscriber.lastName.name}>
          {Subscriber.lastName.label}
        </label>
        <input className="metaobjects-field-input" {...form.input.lastName} />
        {form.formState.errors.lastName !== undefined && (
          <span className="metaobjects-field-error" role="alert">
            {String(form.formState.errors.lastName?.message ?? "")}
          </span>
        )}
      </div>
      <div className="metaobjects-field" key="subscribed">
        <label className="metaobjects-field-label" htmlFor={Subscriber.subscribed.name}>
          {Subscriber.subscribed.label}
        </label>
        <input className="metaobjects-field-input" {...form.input.subscribed} />
        {form.formState.errors.subscribed !== undefined && (
          <span className="metaobjects-field-error" role="alert">
            {String(form.formState.errors.subscribed?.message ?? "")}
          </span>
        )}
      </div>
      <div className="metaobjects-form-actions">
        <button className="metaobjects-form-submit" type="submit" disabled={form.formState.isSubmitting}>
          {props.submitLabel ?? "Submit"}
        </button>
      </div>
    </form>
  );
}
