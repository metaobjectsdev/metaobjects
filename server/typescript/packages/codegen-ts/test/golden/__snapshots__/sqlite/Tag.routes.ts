// @generated by @metaobjects/codegen-ts — DO NOT EDIT.
// Source metadata: Tag (Tag)
// Customize via Tag.extra.ts in this directory (e.g., auth, additional handlers).
import { db } from "~/server/db";
import {
  Tag,
  TagFilterAllowlist,
  TagInsertSchema,
  tags,
  TagSortAllowlist,
  TagUpdateSchema,
} from "./Tag";
import { mountCrudRoutes } from "@metaobjects/runtime-ts/drizzle-fastify";
import type { FastifyInstance } from "fastify";

/**
 * Mount the 5 standard REST endpoints for Tag using Drizzle directly.
 *
 * Customize: register this as-is for stock CRUD, OR import the per-verb
 * helpers (mountListRoute, mountGetRoute, ...) from
 * @metaobjects/runtime-ts/drizzle-fastify and mix with your own handlers
 * (auth, side effects, etc.).
 */
export async function tagRoutes(fastify: FastifyInstance) {
  mountCrudRoutes({
    fastify,
    path: Tag.$path,
    db,
    table: tags,
    insertSchema: TagInsertSchema,
    updateSchema: TagUpdateSchema,
    filterAllowlist: TagFilterAllowlist,
    sortAllowlist: TagSortAllowlist,
    dialect: "sqlite",
  });
}
