// @generated by @metaobjects/codegen-ts — DO NOT EDIT.
// Source metadata: Purchase (Purchase)
// Customize via Purchase.extra.tsx (custom layouts, per-field components, etc.).
import { Purchase, PurchaseInsertSchema } from "./Purchase";
import type { Purchase as PurchaseRow } from "./Purchase";
import { useEntityForm } from "@metaobjects/runtime-ts-client/react";
import type { ReactElement } from "react";
import type { SubmitHandler } from "react-hook-form";

export interface PurchaseFormProps {
  onSubmit: SubmitHandler<Partial<PurchaseRow>>;
  defaultValues?: Partial<PurchaseRow>;
  submitLabel?: string;
  className?: string;
}

/**
 * Generated React form for Purchase. All field metadata (name, label,
 * placeholder, html input type, RHF validation rules) comes from the
 * Purchase constants emitted alongside this file. Validation uses the
 * generated Zod schema, shared with the server-side Fastify route.
 *
 * Customize: this file is regenerated on every `forge gen`. If you need a
 * custom layout, write your own component using useEntityForm + the
 * Purchase constants directly.
 */
export function PurchaseForm(props: PurchaseFormProps): ReactElement {
  const form = useEntityForm(
    Purchase,
    PurchaseInsertSchema,
    props.defaultValues !== undefined ? { defaultValues: props.defaultValues } : {},
  );
  return (
    <form
      className={props.className ?? "metaobjects-form"}
      data-entity={Purchase.$entity}
      onSubmit={form.handleSubmit(props.onSubmit as never)}
    >
      <div className="metaobjects-field" key="subscriberId">
        <label className="metaobjects-field-label" htmlFor={Purchase.subscriberId.name}>
          {Purchase.subscriberId.label}
        </label>
        <input className="metaobjects-field-input" {...form.input.subscriberId} />
        {form.formState.errors.subscriberId !== undefined && (
          <span className="metaobjects-field-error" role="alert">
            {String(form.formState.errors.subscriberId?.message ?? "")}
          </span>
        )}
      </div>
      <div className="metaobjects-field" key="programId">
        <label className="metaobjects-field-label" htmlFor={Purchase.programId.name}>{Purchase.programId.label}</label>
        <input className="metaobjects-field-input" {...form.input.programId} />
        {form.formState.errors.programId !== undefined && (
          <span className="metaobjects-field-error" role="alert">
            {String(form.formState.errors.programId?.message ?? "")}
          </span>
        )}
      </div>
      <div className="metaobjects-field" key="amountCents">
        <label className="metaobjects-field-label" htmlFor={Purchase.amountCents.name}>
          {Purchase.amountCents.label}
        </label>
        <input className="metaobjects-field-input" {...form.input.amountCents} />
        {form.formState.errors.amountCents !== undefined && (
          <span className="metaobjects-field-error" role="alert">
            {String(form.formState.errors.amountCents?.message ?? "")}
          </span>
        )}
      </div>
      <button type="submit" disabled={form.formState.isSubmitting}>{props.submitLabel ?? "Submit"}</button>
    </form>
  );
}
