// @generated by @metaobjectsdev/codegen-ts — DO NOT EDIT.
// Source metadata: Exercise (Exercise)
// Customize via Exercise.extra.tsx (custom layouts, per-field components, etc.).
import { Exercise, ExerciseInsertSchema } from "./Exercise";
import type { Exercise as ExerciseRow } from "./Exercise";
import { useEntityForm } from "@metaobjectsdev/react";
import type { ReactElement } from "react";
import type { SubmitHandler } from "react-hook-form";

export interface ExerciseFormProps {
  onSubmit: SubmitHandler<Partial<ExerciseRow>>;
  defaultValues?: Partial<ExerciseRow>;
  submitLabel?: string;
  className?: string;
}

/**
 * Generated React form for Exercise. All field metadata (name, label,
 * placeholder, html input type, RHF validation rules) comes from the
 * Exercise constants emitted alongside this file. Validation uses the
 * generated Zod schema, shared with the server-side Fastify route.
 *
 * Customize: this file is regenerated on every `forge gen`. If you need a
 * custom layout, write your own component using useEntityForm + the
 * Exercise constants directly.
 */
export function ExerciseForm(props: ExerciseFormProps): ReactElement {
  const form = useEntityForm(
    Exercise,
    ExerciseInsertSchema,
    props.defaultValues !== undefined ? { defaultValues: props.defaultValues } : {},
  );
  return (
    <form
      className={props.className ?? "metaobjects-form"}
      data-entity={Exercise.$entity}
      onSubmit={form.handleSubmit(props.onSubmit as never)}
    >
      <div className="metaobjects-field" key="workoutId">
        <label className="metaobjects-field-label" htmlFor={Exercise.workoutId.name}>{Exercise.workoutId.label}</label>
        <input className="metaobjects-field-input" {...form.input.workoutId} />
        {form.formState.errors.workoutId !== undefined && (
          <span className="metaobjects-field-error" role="alert">
            {String(form.formState.errors.workoutId?.message ?? "")}
          </span>
        )}
      </div>
      <div className="metaobjects-field" key="name">
        <label className="metaobjects-field-label" htmlFor={Exercise.name.name}>{Exercise.name.label}</label>
        <input className="metaobjects-field-input" {...form.input.name} />
        {form.formState.errors.name !== undefined && (
          <span className="metaobjects-field-error" role="alert">
            {String(form.formState.errors.name?.message ?? "")}
          </span>
        )}
      </div>
      <div className="metaobjects-field" key="sets">
        <label className="metaobjects-field-label" htmlFor={Exercise.sets.name}>{Exercise.sets.label}</label>
        <input className="metaobjects-field-input" {...form.input.sets} />
        {form.formState.errors.sets !== undefined && (
          <span className="metaobjects-field-error" role="alert">
            {String(form.formState.errors.sets?.message ?? "")}
          </span>
        )}
      </div>
      <div className="metaobjects-field" key="reps">
        <label className="metaobjects-field-label" htmlFor={Exercise.reps.name}>{Exercise.reps.label}</label>
        <input className="metaobjects-field-input" {...form.input.reps} />
        {form.formState.errors.reps !== undefined && (
          <span className="metaobjects-field-error" role="alert">
            {String(form.formState.errors.reps?.message ?? "")}
          </span>
        )}
      </div>
      <div className="metaobjects-field" key="notes">
        <label className="metaobjects-field-label" htmlFor={Exercise.notes.name}>{Exercise.notes.label}</label>
        <input className="metaobjects-field-input" {...form.input.notes} />
        {form.formState.errors.notes !== undefined && (
          <span className="metaobjects-field-error" role="alert">
            {String(form.formState.errors.notes?.message ?? "")}
          </span>
        )}
      </div>
      <button type="submit" disabled={form.formState.isSubmitting}>{props.submitLabel ?? "Submit"}</button>
    </form>
  );
}
