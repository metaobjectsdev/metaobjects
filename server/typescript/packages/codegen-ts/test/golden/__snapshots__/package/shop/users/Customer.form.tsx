// @generated by @metaobjects/codegen-ts — DO NOT EDIT.
// Source metadata: Customer (shop::users::Customer)
// Customize via Customer.extra.tsx (custom layouts, per-field components, etc.).
import { Customer, CustomerInsertSchema } from "./Customer";
import type { Customer as CustomerRow } from "./Customer";
import { useEntityForm } from "@metaobjects/runtime-ts-client/react";
import type { ReactElement } from "react";
import type { SubmitHandler } from "react-hook-form";

export interface CustomerFormProps {
  onSubmit: SubmitHandler<Partial<CustomerRow>>;
  defaultValues?: Partial<CustomerRow>;
  submitLabel?: string;
  className?: string;
}

/**
 * Generated React form for Customer. All field metadata (name, label,
 * placeholder, html input type, RHF validation rules) comes from the
 * Customer constants emitted alongside this file. Validation uses the
 * generated Zod schema, shared with the server-side Fastify route.
 *
 * Customize: this file is regenerated on every `forge gen`. If you need a
 * custom layout, write your own component using useEntityForm + the
 * Customer constants directly.
 */
export function CustomerForm(props: CustomerFormProps): ReactElement {
  const form = useEntityForm(
    Customer,
    CustomerInsertSchema,
    props.defaultValues !== undefined ? { defaultValues: props.defaultValues } : {},
  );
  return (
    <form
      className={props.className ?? "metaobjects-form"}
      data-entity={Customer.$entity}
      onSubmit={form.handleSubmit(props.onSubmit as never)}
    >
      <div className="metaobjects-field" key="email">
        <label className="metaobjects-field-label" htmlFor={Customer.email.name}>{Customer.email.label}</label>
        <input className="metaobjects-field-input" {...form.input.email} />
        {form.formState.errors.email !== undefined && (
          <span className="metaobjects-field-error" role="alert">
            {String(form.formState.errors.email?.message ?? "")}
          </span>
        )}
      </div>
      <button type="submit" disabled={form.formState.isSubmitting}>{props.submitLabel ?? "Submit"}</button>
    </form>
  );
}
