// @generated by @metaobjectsdev/codegen-ts — DO NOT EDIT.
// Source metadata: Product (shop::commerce::Product)
// Customize via Product.extra.ts in this directory (e.g., auth, additional handlers).
import { db } from "../../../db";
import {
  Product,
  ProductFilterAllowlist,
  ProductInsertSchema,
  products,
  ProductSortAllowlist,
  ProductUpdateSchema,
} from "./Product";
import { mountCrudRoutes } from "@metaobjectsdev/runtime-ts/drizzle-fastify";
import type { FastifyInstance } from "fastify";

/**
 * Mount the 5 standard REST endpoints for Product using Drizzle directly.
 *
 * Customize: register this as-is for stock CRUD, OR import the per-verb
 * helpers (mountListRoute, mountGetRoute, ...) from
 * @metaobjectsdev/runtime-ts/drizzle-fastify and mix with your own handlers
 * (auth, side effects, etc.).
 */
export async function productRoutes(fastify: FastifyInstance) {
  mountCrudRoutes({
    fastify,
    path: Product.$path,
    db,
    table: products,
    insertSchema: ProductInsertSchema,
    updateSchema: ProductUpdateSchema,
    filterAllowlist: ProductFilterAllowlist,
    sortAllowlist: ProductSortAllowlist,
    dialect: "sqlite",
  });
}
