// @generated by @metaobjectsdev/codegen-ts — DO NOT EDIT.
// Source metadata: Tag (Tag)
// Customize via Tag.extra.tsx (custom layouts, per-field components, etc.).
import { Tag, TagInsertSchema } from "./Tag";
import type { Tag as TagRow } from "./Tag";
import { useEntityForm } from "@metaobjectsdev/react";
import type { ReactElement } from "react";
import type { SubmitHandler } from "react-hook-form";

export interface TagFormProps {
  onSubmit: SubmitHandler<Partial<TagRow>>;
  defaultValues?: Partial<TagRow>;
  submitLabel?: string;
  className?: string;
}

/**
 * Generated React form for Tag. All field metadata (name, label,
 * placeholder, html input type, RHF validation rules) comes from the
 * Tag constants emitted alongside this file. Validation uses the
 * generated Zod schema, shared with the server-side Fastify route.
 *
 * Customize: this file is regenerated on every `forge gen`. If you need a
 * custom layout, write your own component using useEntityForm + the
 * Tag constants directly.
 */
export function TagForm(props: TagFormProps): ReactElement {
  const form = useEntityForm(
    Tag,
    TagInsertSchema,
    props.defaultValues !== undefined ? { defaultValues: props.defaultValues } : {},
  );
  return (
    <form
      className={props.className ?? "metaobjects-form"}
      data-entity={Tag.$entity}
      onSubmit={form.handleSubmit(props.onSubmit as never)}
    >
      <div className="metaobjects-field" key="label">
        <label className="metaobjects-field-label" htmlFor={Tag.label.name}>{Tag.label.label}</label>
        <input className="metaobjects-field-input" {...form.input.label} />
        {form.formState.errors.label !== undefined && (
          <span className="metaobjects-field-error" role="alert">
            {String(form.formState.errors.label?.message ?? "")}
          </span>
        )}
      </div>
      <div className="metaobjects-field" key="customerId">
        <label className="metaobjects-field-label" htmlFor={Tag.customerId.name}>{Tag.customerId.label}</label>
        <input className="metaobjects-field-input" {...form.input.customerId} />
        {form.formState.errors.customerId !== undefined && (
          <span className="metaobjects-field-error" role="alert">
            {String(form.formState.errors.customerId?.message ?? "")}
          </span>
        )}
      </div>
      <div className="metaobjects-form-actions">
        <button className="metaobjects-form-submit" type="submit" disabled={form.formState.isSubmitting}>
          {props.submitLabel ?? "Submit"}
        </button>
      </div>
    </form>
  );
}
