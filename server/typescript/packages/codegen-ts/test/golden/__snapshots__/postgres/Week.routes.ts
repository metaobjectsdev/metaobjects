// @generated by @metaobjectsdev/codegen-ts — DO NOT EDIT.
// Source metadata: Week (Week)
// Customize via Week.extra.ts in this directory (e.g., auth, additional handlers).
import { db } from "~/server/db";
import {
  Week,
  WeekFilterAllowlist,
  WeekInsertSchema,
  weeks,
  WeekSortAllowlist,
  WeekUpdateSchema,
} from "./Week";
import { mountCrudRoutes } from "@metaobjectsdev/runtime-ts/drizzle-fastify";
import type { FastifyInstance } from "fastify";

/**
 * Mount the 5 standard REST endpoints for Week using Drizzle directly.
 *
 * Customize: register this as-is for stock CRUD, OR import the per-verb
 * helpers (mountListRoute, mountGetRoute, ...) from
 * @metaobjectsdev/runtime-ts/drizzle-fastify and mix with your own handlers
 * (auth, side effects, etc.).
 */
export async function weekRoutes(fastify: FastifyInstance) {
  mountCrudRoutes({
    fastify,
    path: Week.$path,
    db,
    table: weeks,
    insertSchema: WeekInsertSchema,
    updateSchema: WeekUpdateSchema,
    filterAllowlist: WeekFilterAllowlist,
    sortAllowlist: WeekSortAllowlist,
    dialect: "postgres",
  });
}
