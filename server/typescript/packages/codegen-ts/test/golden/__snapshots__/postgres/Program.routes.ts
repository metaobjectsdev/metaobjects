// @generated by @metaobjectsdev/codegen-ts — DO NOT EDIT.
// Source metadata: Program (Program)
// Customize via Program.extra.ts in this directory (e.g., auth, additional handlers).
import { db } from "~/server/db";
import {
  Program,
  ProgramFilterAllowlist,
  ProgramInsertSchema,
  programs,
  ProgramSortAllowlist,
  ProgramUpdateSchema,
} from "./Program";
import { mountCrudRoutes } from "@metaobjectsdev/runtime-ts/drizzle-fastify";
import type { FastifyInstance } from "fastify";

/**
 * Mount the 5 standard REST endpoints for Program using Drizzle directly.
 *
 * Customize: register this as-is for stock CRUD, OR import the per-verb
 * helpers (mountListRoute, mountGetRoute, ...) from
 * @metaobjectsdev/runtime-ts/drizzle-fastify and mix with your own handlers
 * (auth, side effects, etc.).
 */
export async function programRoutes(fastify: FastifyInstance) {
  mountCrudRoutes({
    fastify,
    path: Program.$path,
    db,
    table: programs,
    insertSchema: ProgramInsertSchema,
    updateSchema: ProgramUpdateSchema,
    filterAllowlist: ProgramFilterAllowlist,
    sortAllowlist: ProgramSortAllowlist,
    dialect: "postgres",
  });
}
