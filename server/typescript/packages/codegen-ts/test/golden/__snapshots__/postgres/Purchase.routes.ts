// @generated by @metaobjects/codegen-ts — DO NOT EDIT.
// Source metadata: Purchase (Purchase)
// Customize via Purchase.extra.ts in this directory (e.g., auth, additional handlers).
import { db } from "~/server/db";
import {
  Purchase,
  PurchaseFilterAllowlist,
  PurchaseInsertSchema,
  purchases,
  PurchaseSortAllowlist,
  PurchaseUpdateSchema,
} from "./Purchase";
import { mountCrudRoutes } from "@metaobjects/runtime-ts/drizzle-fastify";
import type { FastifyInstance } from "fastify";

/**
 * Mount the 5 standard REST endpoints for Purchase using Drizzle directly.
 *
 * Customize: register this as-is for stock CRUD, OR import the per-verb
 * helpers (mountListRoute, mountGetRoute, ...) from
 * @metaobjects/runtime-ts/drizzle-fastify and mix with your own handlers
 * (auth, side effects, etc.).
 */
export async function purchaseRoutes(fastify: FastifyInstance) {
  mountCrudRoutes({
    fastify,
    path: Purchase.$path,
    db,
    table: purchases,
    insertSchema: PurchaseInsertSchema,
    updateSchema: PurchaseUpdateSchema,
    filterAllowlist: PurchaseFilterAllowlist,
    sortAllowlist: PurchaseSortAllowlist,
    dialect: "postgres",
  });
}
