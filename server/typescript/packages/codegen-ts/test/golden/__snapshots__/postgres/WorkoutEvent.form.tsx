// @generated by @metaobjectsdev/codegen-ts — DO NOT EDIT.
// Source metadata: WorkoutEvent (WorkoutEvent)
// Customize via WorkoutEvent.extra.tsx (custom layouts, per-field components, etc.).
import { WorkoutEvent, WorkoutEventInsertSchema, WorkoutEventUpdateSchema } from "./WorkoutEvent";
import type { WorkoutEvent as WorkoutEventRow } from "./WorkoutEvent";
import { useEntityForm } from "@metaobjectsdev/react";
import type { ReactElement } from "react";
import type { SubmitHandler } from "react-hook-form";

const BLANK_OPTIONAL_FIELDS = ["durationMinutes"] as const;

/**
 * Normalize blank optional inputs on the way out of the form (#223).
 * On create a blank field is OMITTED (the column defaults); on edit it is sent as
 * explicit `null` (present-null clears, per the FR-035 PATCH tristate).
 */
function normalizeBlankOptionals(values: Record<string, unknown>, isEdit: boolean): Record<string, unknown> {
  const out: Record<string, unknown> = { ...values };
  for (const key of BLANK_OPTIONAL_FIELDS) {
    if (out[key] !== "") {
      continue;
    }
    if (isEdit) {
      out[key] = null;
    } else {
      delete out[key];
    }
  }
  return out;
}

export interface WorkoutEventFormProps {
  onSubmit: SubmitHandler<Partial<WorkoutEventRow>>;
  defaultValues?: Partial<WorkoutEventRow>;
  submitLabel?: string;
  className?: string;
}

/**
 * Generated React form for WorkoutEvent. All field metadata (name, label,
 * placeholder, html input type, RHF validation rules) comes from the
 * WorkoutEvent constants emitted alongside this file. Validation uses the
 * generated Zod schema, shared with the server-side Fastify route.
 *
 * Customize: this file is regenerated on every `forge gen`. If you need a
 * custom layout, write your own component using useEntityForm + the
 * WorkoutEvent constants directly.
 */
export function WorkoutEventForm(props: WorkoutEventFormProps): ReactElement {
  const form = useEntityForm(
    WorkoutEvent,
    props.defaultValues !== undefined ? WorkoutEventUpdateSchema : WorkoutEventInsertSchema,
    props.defaultValues !== undefined ? { defaultValues: props.defaultValues } : {},
  );
  return (
    <form
      className={props.className ?? "metaobjects-form"}
      data-entity={WorkoutEvent.$entity}
      onSubmit={form.handleSubmit(
        ((values: Record<string, unknown>, event?: unknown) =>
          props.onSubmit(
            normalizeBlankOptionals(values, props.defaultValues !== undefined) as never,
            event as never,
          )) as never,
      )}
    >
      <div className="metaobjects-field" key="subscriberId">
        <label className="metaobjects-field-label" htmlFor={WorkoutEvent.subscriberId.name}>
          {WorkoutEvent.subscriberId.label}
        </label>
        <input className="metaobjects-field-input" {...form.input.subscriberId} />
        {form.formState.errors.subscriberId !== undefined && (
          <span className="metaobjects-field-error" role="alert">
            {String(form.formState.errors.subscriberId?.message ?? "")}
          </span>
        )}
      </div>
      <div className="metaobjects-field" key="workoutId">
        <label className="metaobjects-field-label" htmlFor={WorkoutEvent.workoutId.name}>
          {WorkoutEvent.workoutId.label}
        </label>
        <input className="metaobjects-field-input" {...form.input.workoutId} />
        {form.formState.errors.workoutId !== undefined && (
          <span className="metaobjects-field-error" role="alert">
            {String(form.formState.errors.workoutId?.message ?? "")}
          </span>
        )}
      </div>
      <div className="metaobjects-field" key="durationMinutes">
        <label className="metaobjects-field-label" htmlFor={WorkoutEvent.durationMinutes.name}>
          {WorkoutEvent.durationMinutes.label}
        </label>
        <input className="metaobjects-field-input" {...form.input.durationMinutes} />
        {form.formState.errors.durationMinutes !== undefined && (
          <span className="metaobjects-field-error" role="alert">
            {String(form.formState.errors.durationMinutes?.message ?? "")}
          </span>
        )}
      </div>
      <div className="metaobjects-form-actions">
        <button className="metaobjects-form-submit" type="submit" disabled={form.formState.isSubmitting}>
          {props.submitLabel ?? "Submit"}
        </button>
      </div>
    </form>
  );
}
