// @generated by @metaobjectsdev/codegen-ts — DO NOT EDIT.
// Source metadata: Exercise (Exercise)
// Customize via Exercise.extra.ts in this directory.
import {
  type InferInsertModel,
  type InferSelectModel,
  relations,
} from "drizzle-orm";
import {
  type AnyPgColumn,
  bigint,
  bigserial,
  integer,
  pgTable,
  text,
  varchar,
} from "drizzle-orm/pg-core";
import { z } from "zod";
import { workouts } from "./Workout";

export const exercises = pgTable("exercises", {
  id: bigserial("id", { mode: "number" }).primaryKey(),
  workoutId: bigint("workout_id", { mode: "number" })
    .notNull()
    .references((): AnyPgColumn => workouts.id),
  name: varchar("name", { length: 200 }).notNull(),
  sets: integer("sets"),
  reps: integer("reps"),
  notes: text("notes"),
});
export const exercisesRelations = relations(exercises, ({ one }) => ({
  workout: one(workouts, {
    fields: [exercises.workoutId],
    references: [workouts.id],
  }),
}));
export type Exercise = InferSelectModel<typeof exercises>;
export type ExerciseInsert = InferInsertModel<typeof exercises>;
export type ExerciseUpdate = Partial<ExerciseInsert>;
export const ExerciseInsertSchema = z.object({
  workoutId: z.number().int(),
  name: z.string().min(1).max(200),
  sets: z.number().int().optional(),
  reps: z.number().int().optional(),
  notes: z.string().optional(),
});

export const ExerciseUpdateSchema = z.object({
  workoutId: z.number().int().optional(),
  name: z.string().min(1).max(200).optional(),
  sets: z.number().int().optional().nullable(),
  reps: z.number().int().optional().nullable(),
  notes: z.string().optional().nullable(),
});

/** Typed patch shape for Exercise: every settable field, optional (FR-035 PATCH). A
 * renamed/dropped field is a compile error at every `updateExercise` call site. */
export type ExercisePatch = z.input<typeof ExerciseUpdateSchema>;
/**
 * Metadata constants for Exercise.
 *
 * Use these instead of magic strings so TS catches typos and refactors stay
 * coherent. Each non-dollar-prefixed key is a per-field object carrying
 * name, label, view, and the RHF-shaped validation rules derived from the
 * field's validator children.
 *
 * htmlType, placeholder and helpText are NOT emitted — no provider registers
 * an attribute for them, so nothing here could derive one. useEntityForm
 * still honours all three if you add them: this file is generated, hand edits
 * inside it survive regeneration through the three-way merge, and the
 * consumers read this const rather than the metadata. That is the intended
 * way to set them.
 *
 * Typical usage with the metaobjects React form helper:
 *
 *   import { useEntityForm } from '@metaobjectsdev/react';
 *   const form = useEntityForm(Exercise, ExerciseInsertSchema);
 *   <input {...form.input.id} />
 */
export const Exercise = {
  $entity: "Exercise",
  $table: "exercises",
  $path: "/exercises",
  id: { name: "id", label: "Id", view: "number", htmlType: "number" },
  workoutId: {
    name: "workoutId",
    label: "Workout Id",
    view: "number",
    htmlType: "number",
    rules: { required: "Workout Id is required" },
  },
  name: {
    name: "name",
    label: "Name",
    view: "text",
    htmlType: "text",
    rules: {
      required: "Name is required",
      maxLength: { value: 200, message: "Must be 200 characters or fewer" },
    },
  },
  sets: { name: "sets", label: "Sets", view: "number", htmlType: "number" },
  reps: { name: "reps", label: "Reps", view: "number", htmlType: "number" },
  notes: { name: "notes", label: "Notes", view: "text", htmlType: "text" },
} as const;
import type { FilterAllowlist } from "@metaobjectsdev/runtime-ts/drizzle-fastify";

export const ExerciseFilterAllowlist = {} as const satisfies FilterAllowlist;
import type { SortAllowlist } from "@metaobjectsdev/runtime-ts/drizzle-fastify";

export const ExerciseSortAllowlist = {} as const satisfies SortAllowlist;
export type ExerciseFilter = {
  limit?: number;
  offset?: number;
  sort?: string;
  or?: ExerciseFilter[];
  and?: ExerciseFilter[];
};
