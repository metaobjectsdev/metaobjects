{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": ["src/**/*"],
  "// src/reference": "ADR-0034 copyable reference templates — scaffold ASSETS, not package source. They import '@metaobjectsdev/codegen-ts' and '@metaobjectsdev/codegen-ts-tanstack' (the consumer's view), so they are excluded from the package build and verified separately against the built public API.",
  "exclude": ["dist", "test", "node_modules", "src/reference"]
}
