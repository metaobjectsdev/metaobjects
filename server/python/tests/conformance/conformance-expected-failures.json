{
  "language": "python",
  "fixtures": [],
  "notes": "No expected conformance failures. The AI LLM-call trace fixtures ai-trace-prompt-nested and ai-trace-sti — previously ledgered TS-pilot gaps — now load + canonical-serialize cleanly in the Python port: @responseRef is registered on template.prompt (carved out of the cross-port registry manifest as ExclusionReason.TS_PILOT_VOCAB until the atomic all-ports flip), and template.* is admitted as an object.entity child (_OBJECT_CHILD_RULES). The sibling ai-llm-call-extends has always passed cross-port."
}
