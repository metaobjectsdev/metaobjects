{
  "language": "python",
  "fixtures": ["ai-trace-prompt-nested", "ai-trace-sti"],
  "notes": "FR-013 (@readOnly), FR-014 (TPH discriminator), and FR-015 (@parameterRef) validation passes shipped in the Python port (loader/validate_field_readonly.py, validate_discriminator.py, validate_source_parameter_ref.py); all error-* fixtures now produce the expected envelopes. ai-trace-prompt-nested exercises the AI #1b prompt-derived-trace vertical, a deliberate TS pilot (cross-language LLM-call trace persistence): it places a template.prompt CHILD under object.entity and uses @responseRef on template.prompt — neither is yet cross-port (entity child rules do not admit template.* children; @responseRef is carved out of the cross-port registry manifest as TsPilotVocab). Under strict ADR-0023 load this happy-path fixture reports ERR_UNKNOWN here. Ledgered until the AI vertical is promoted from the TS pilot to all five ports. The sibling fixture ai-llm-call-extends (plain entity + extends) passes cross-port and is NOT ledgered. ai-trace-sti is the same TS pilot in its shared-table STI (#1c) authoring shape: a TPH base PromptTrace (@discriminator) over abstract LlmCallBase, with concrete subtypes ClassifyCall/SummarizeCall each carrying @discriminatorValue plus a nested template.prompt (@textRef/@payloadRef/@responseRef). Same cross-port gap (template.* child under object.entity + @responseRef TsPilotVocab); ledgered until the AI vertical is promoted to all five ports."
}
