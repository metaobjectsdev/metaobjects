package acme.auth

/**
 * GENERATED — per-entity FR-009 filter allowlist for AuthLine.
 * FIELDS lists the filterable field names; OPS_BY_FIELD constrains the
 * operator vocabulary for each field by its subtype.
 */
object AuthLineFilterAllowlist {
    val FIELDS: Set<String> = emptySet()

    val OPS_BY_FIELD: Map<String, Set<String>> = emptyMap()
}
