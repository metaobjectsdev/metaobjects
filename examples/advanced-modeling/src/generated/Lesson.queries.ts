// @generated by @metaobjectsdev/codegen-ts — DO NOT EDIT.
// Source metadata: Lesson (Lesson)
// Customize via Lesson.extra.ts in this directory (additional queries, custom logic).
import { eq, inArray } from "drizzle-orm";

import type { PgDatabase, PgQueryResultHKT } from "drizzle-orm/pg-core";
type Db = PgDatabase<PgQueryResultHKT, Record<string, never>>;

import {
  type Lesson,
  LessonInsertSchema,
  type LessonPatch,
  lessons,
  LessonUpdateSchema,
} from "./Lesson";
export async function findLessonById(
  db: Db,
  id: string,
): Promise<Lesson | null> {
  const [lesson] = await db
    .select()
    .from(lessons)
    .where(eq(lessons.id, id))
    .limit(1);
  return lesson ?? null;
}
export async function listLessons(
  db: Db,
  opts?: { limit?: number; offset?: number },
): Promise<Lesson[]> {
  let q = db.select().from(lessons).$dynamic();
  if (opts?.limit !== undefined) {
    q = q.limit(opts.limit);
  }
  if (opts?.offset !== undefined) {
    q = q.offset(opts.offset);
  }
  return q;
}
export async function createLesson(db: Db, data: unknown): Promise<Lesson> {
  const validated = LessonInsertSchema.parse(data);
  const [lesson] = await db.insert(lessons).values(validated).returning();
  return lesson!;
}
export async function updateLesson(
  db: Db,
  id: string,
  patch: LessonPatch,
): Promise<Lesson | null> {
  const validated = LessonUpdateSchema.parse(patch);
  // PATCH-5: an empty patch is a no-op — return the current row rather than let
  // Drizzle throw on an empty SET clause.
  if (Object.keys(validated).length === 0) {
    return findLessonById(db, id);
  }
  const [lesson] = await db
    .update(lessons)
    .set(validated)
    .where(eq(lessons.id, id))
    .returning();
  return lesson ?? null;
}
export async function deleteLessonById(db: Db, id: string): Promise<boolean> {
  // Use .returning() unconditionally — supported on SQLite ≥3.35 (covers D1, libsql/Turso)
  // and Postgres. Result is an array of deleted rows; presence implies success.
  const deleted = await db
    .delete(lessons)
    .where(eq(lessons.id, id))
    .returning();
  return deleted.length > 0;
}
export async function findLessonsByProgram(
  db: Db,
  programId: string,
): Promise<Lesson[]> {
  return db.select().from(lessons).where(eq(lessons.programId, programId));
}

export async function findLessonsByProgramIn(
  db: Db,
  programIds: string[],
): Promise<Lesson[]> {
  if (programIds.length === 0) {
    return [];
  }
  return db
    .select()
    .from(lessons)
    .where(inArray(lessons.programId, programIds));
}
