// @generated by @metaobjectsdev/codegen-ts — DO NOT EDIT.
// Source metadata: Lesson (Lesson)
// Customize via Lesson.extra.tsx (custom layouts, per-field components, etc.).
import { Lesson, LessonInsertSchema, LessonUpdateSchema } from "./Lesson";
import type { Lesson as LessonRow } from "./Lesson";
import { useEntityForm } from "@metaobjectsdev/react";
import type { ReactElement } from "react";
import type { SubmitHandler } from "react-hook-form";

export interface LessonFormProps {
  onSubmit: SubmitHandler<Partial<LessonRow>>;
  defaultValues?: Partial<LessonRow>;
  submitLabel?: string;
  className?: string;
}

/**
 * Generated React form for Lesson. All field metadata (name, label,
 * placeholder, html input type, RHF validation rules) comes from the
 * Lesson constants emitted alongside this file. Validation uses the
 * generated Zod schema, shared with the server-side Fastify route.
 *
 * Customize: this file is regenerated on every `forge gen`. If you need a
 * custom layout, write your own component using useEntityForm + the
 * Lesson constants directly.
 */
export function LessonForm(props: LessonFormProps): ReactElement {
  const form = useEntityForm(
    Lesson,
    props.defaultValues !== undefined ? LessonUpdateSchema : LessonInsertSchema,
    props.defaultValues !== undefined ? { defaultValues: props.defaultValues } : {},
  );
  return (
    <form
      className={props.className ?? "metaobjects-form"}
      data-entity={Lesson.$entity}
      onSubmit={form.handleSubmit(props.onSubmit as never)}
    >
      <div className="metaobjects-field" key="programId">
        <label className="metaobjects-field-label" htmlFor={Lesson.programId.name}>{Lesson.programId.label}</label>
        <input className="metaobjects-field-input" {...form.input.programId} />
        {form.formState.errors.programId !== undefined && (
          <span className="metaobjects-field-error" role="alert">
            {String(form.formState.errors.programId?.message ?? "")}
          </span>
        )}
      </div>
      <div className="metaobjects-field" key="title">
        <label className="metaobjects-field-label" htmlFor={Lesson.title.name}>{Lesson.title.label}</label>
        <input className="metaobjects-field-input" {...form.input.title} />
        {form.formState.errors.title !== undefined && (
          <span className="metaobjects-field-error" role="alert">
            {String(form.formState.errors.title?.message ?? "")}
          </span>
        )}
      </div>
      <div className="metaobjects-field" key="position">
        <label className="metaobjects-field-label" htmlFor={Lesson.position.name}>{Lesson.position.label}</label>
        <input className="metaobjects-field-input" {...form.input.position} />
        {form.formState.errors.position !== undefined && (
          <span className="metaobjects-field-error" role="alert">
            {String(form.formState.errors.position?.message ?? "")}
          </span>
        )}
      </div>
      <div className="metaobjects-form-actions">
        <button className="metaobjects-form-submit" type="submit" disabled={form.formState.isSubmitting}>
          {props.submitLabel ?? "Submit"}
        </button>
      </div>
    </form>
  );
}
