// @generated by @metaobjectsdev/codegen-ts — hand edits are kept by a three-way merge on regeneration; meta verify --codegen checks only the generated parts.
// Source metadata: InstructorProfile (InstructorProfile)
// Extend in your own module (e.g. InstructorProfile.extra.ts) — nothing imports it for you.
import { z } from "zod";

export interface InstructorProfile {
  displayName: string;
  bio?: string | undefined;
  credentialsSummary?: string | undefined;
}
export const InstructorProfileInsertSchema = z.object({
  displayName: z.string().min(1).max(120),
  bio: z.string().max(2000).optional(),
  credentialsSummary: z.string().max(300).optional(),
});
