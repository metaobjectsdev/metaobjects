// @generated by @metaobjectsdev/codegen-ts — DO NOT EDIT.
// Source metadata: Author (Author)
// Customize via Author.extra.tsx (custom layouts, per-field components, etc.).
import { Author, AuthorInsertSchema, AuthorUpdateSchema } from "./Author";
import type { Author as AuthorRow } from "./Author";
import { useEntityForm } from "@metaobjectsdev/react";
import type { ReactElement } from "react";
import type { SubmitHandler } from "react-hook-form";

const BLANK_OPTIONAL_FIELDS = ["bio"] as const;

/**
 * Normalize blank optional inputs on the way out of the form (#223).
 * On create a blank field is OMITTED (the column defaults); on edit it is sent as
 * explicit `null` (present-null clears, per the FR-035 PATCH tristate).
 */
function normalizeBlankOptionals(values: Record<string, unknown>, isEdit: boolean): Record<string, unknown> {
  const out: Record<string, unknown> = { ...values };
  for (const key of BLANK_OPTIONAL_FIELDS) {
    if (out[key] !== "") {
      continue;
    }
    if (isEdit) {
      out[key] = null;
    } else {
      delete out[key];
    }
  }
  return out;
}

export interface AuthorFormProps {
  onSubmit: SubmitHandler<Partial<AuthorRow>>;
  defaultValues?: Partial<AuthorRow>;
  submitLabel?: string;
  className?: string;
}

/**
 * Generated React form for Author. All field metadata (name, label,
 * placeholder, html input type, RHF validation rules) comes from the
 * Author constants emitted alongside this file. Validation uses the
 * generated Zod schema, shared with the server-side Fastify route.
 *
 * Customize: this file is regenerated on every `forge gen`. If you need a
 * custom layout, write your own component using useEntityForm + the
 * Author constants directly.
 */
export function AuthorForm(props: AuthorFormProps): ReactElement {
  const form = useEntityForm(
    Author,
    props.defaultValues !== undefined ? AuthorUpdateSchema : AuthorInsertSchema,
    props.defaultValues !== undefined ? { defaultValues: props.defaultValues } : {},
  );
  return (
    <form
      className={props.className ?? "metaobjects-form"}
      data-entity={Author.$entity}
      onSubmit={form.handleSubmit(
        ((values: Record<string, unknown>, event?: unknown) =>
          props.onSubmit(
            normalizeBlankOptionals(values, props.defaultValues !== undefined) as never,
            event as never,
          )) as never,
      )}
    >
      <div className="metaobjects-field" key="name">
        <label className="metaobjects-field-label" htmlFor={Author.name.name}>{Author.name.label}</label>
        <input className="metaobjects-field-input" {...form.input.name} />
        {form.formState.errors.name !== undefined && (
          <span className="metaobjects-field-error" role="alert">
            {String(form.formState.errors.name?.message ?? "")}
          </span>
        )}
      </div>
      <div className="metaobjects-field" key="email">
        <label className="metaobjects-field-label" htmlFor={Author.email.name}>{Author.email.label}</label>
        <input className="metaobjects-field-input" {...form.input.email} />
        {form.formState.errors.email !== undefined && (
          <span className="metaobjects-field-error" role="alert">
            {String(form.formState.errors.email?.message ?? "")}
          </span>
        )}
      </div>
      <div className="metaobjects-field" key="bio">
        <label className="metaobjects-field-label" htmlFor={Author.bio.name}>{Author.bio.label}</label>
        <input className="metaobjects-field-input" {...form.input.bio} />
        {form.formState.errors.bio !== undefined && (
          <span className="metaobjects-field-error" role="alert">
            {String(form.formState.errors.bio?.message ?? "")}
          </span>
        )}
      </div>
      <div className="metaobjects-form-actions">
        <button className="metaobjects-form-submit" type="submit" disabled={form.formState.isSubmitting}>
          {props.submitLabel ?? "Submit"}
        </button>
      </div>
    </form>
  );
}
