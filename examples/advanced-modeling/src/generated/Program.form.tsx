// @generated by @metaobjectsdev/codegen-ts — DO NOT EDIT.
// Source metadata: Program (Program)
// Customize via Program.extra.tsx (custom layouts, per-field components, etc.).
import { ImageUpload } from "@metaobjectsdev/react";
import { useFieldArray } from "react-hook-form";
import { Controller } from "react-hook-form";
import { Program, ProgramInsertSchema, ProgramUpdateSchema } from "./Program";
import type { Program as ProgramRow } from "./Program";
import { useEntityForm } from "@metaobjectsdev/react";
import type { ReactElement } from "react";
import type { SubmitHandler } from "react-hook-form";

const BLANK_OPTIONAL_FIELDS = ["status", "summary", "priceCents"] as const;

/**
 * Normalize blank optional inputs on the way out of the form (#223).
 * On create a blank field is OMITTED (the column defaults); on edit it is sent as
 * explicit `null` (present-null clears, per the FR-035 PATCH tristate).
 */
function normalizeBlankOptionals(values: Record<string, unknown>, isEdit: boolean): Record<string, unknown> {
  const out: Record<string, unknown> = { ...values };
  for (const key of BLANK_OPTIONAL_FIELDS) {
    if (out[key] !== "") {
      continue;
    }
    if (isEdit) {
      out[key] = null;
    } else {
      delete out[key];
    }
  }
  return out;
}

export interface ProgramFormProps {
  onSubmit: SubmitHandler<Partial<ProgramRow>>;
  defaultValues?: Partial<ProgramRow>;
  submitLabel?: string;
  className?: string;
}

/**
 * Generated React form for Program. All field metadata (name, label,
 * placeholder, html input type, RHF validation rules) comes from the
 * Program constants emitted alongside this file. Validation uses the
 * generated Zod schema, shared with the server-side Fastify route.
 *
 * Customize: this file is regenerated on every `forge gen`. If you need a
 * custom layout, write your own component using useEntityForm + the
 * Program constants directly.
 */
export function ProgramForm(props: ProgramFormProps): ReactElement {
  const form = useEntityForm(
    Program,
    props.defaultValues !== undefined ? ProgramUpdateSchema : ProgramInsertSchema,
    props.defaultValues !== undefined ? { defaultValues: props.defaultValues } : {},
  );
  const syllabusArray = useFieldArray({ control: form.control, name: "syllabus" as never });
  return (
    <form
      className={props.className ?? "metaobjects-form"}
      data-entity={Program.$entity}
      onSubmit={form.handleSubmit(
        ((values: Record<string, unknown>, event?: unknown) =>
          props.onSubmit(
            normalizeBlankOptionals(values, props.defaultValues !== undefined) as never,
            event as never,
          )) as never,
      )}
    >
      <div className="metaobjects-field" key="title">
        <label className="metaobjects-field-label" htmlFor={Program.title.name}>{Program.title.label}</label>
        <input className="metaobjects-field-input" {...form.input.title} />
        {form.formState.errors.title !== undefined && (
          <span className="metaobjects-field-error" role="alert">
            {String(form.formState.errors.title?.message ?? "")}
          </span>
        )}
      </div>
      <div className="metaobjects-field" key="status">
        <label className="metaobjects-field-label" htmlFor={Program.status.name}>{Program.status.label}</label>
        <select aria-label={Program.status.label} className="metaobjects-field-input" {...form.register("status")}>
          <option value="">Select…</option>
          <option value="draft">draft</option>
          <option value="published">published</option>
          <option value="archived">archived</option>
        </select>
        {form.formState.errors.status !== undefined && (
          <span className="metaobjects-field-error" role="alert">
            {String(form.formState.errors.status?.message ?? "")}
          </span>
        )}
      </div>
      <div className="metaobjects-field" key="summary">
        <label className="metaobjects-field-label" htmlFor={Program.summary.name}>{Program.summary.label}</label>
        <textarea
          aria-label={Program.summary.label}
          className="metaobjects-field-input"
          rows={6}
          {...form.register("summary")}
        />
        {form.formState.errors.summary !== undefined && (
          <span className="metaobjects-field-error" role="alert">
            {String(form.formState.errors.summary?.message ?? "")}
          </span>
        )}
      </div>
      <div className="metaobjects-field" key="priceCents">
        <label className="metaobjects-field-label" htmlFor={Program.priceCents.name}>{Program.priceCents.label}</label>
        <input className="metaobjects-field-input" {...form.input.priceCents} />
        {form.formState.errors.priceCents !== undefined && (
          <span className="metaobjects-field-error" role="alert">
            {String(form.formState.errors.priceCents?.message ?? "")}
          </span>
        )}
      </div>
      <div className="metaobjects-field" key="coverKey">
        <label className="metaobjects-field-label" htmlFor={Program.coverKey.name}>{Program.coverKey.label}</label>
        <Controller
          name="coverKey"
          control={form.control}
          render={({ field: f }) => (
            <ImageUpload
              value={f.value as string | null}
              onChange={f.onChange}
              meta={{
                aspectRatio: 1.777,
                maxEdge: 2000,
                store: "covers",
                accept: ["image/jpeg", "image/png"],
                maxBytes: 10485760,
              }}
            />
          )}
        />
        {form.formState.errors.coverKey !== undefined && (
          <span className="metaobjects-field-error" role="alert">
            {String(form.formState.errors.coverKey?.message ?? "")}
          </span>
        )}
      </div>
      <div className="metaobjects-field" key="authorId">
        <label className="metaobjects-field-label" htmlFor={Program.authorId.name}>{Program.authorId.label}</label>
        <input className="metaobjects-field-input" {...form.input.authorId} />
        {form.formState.errors.authorId !== undefined && (
          <span className="metaobjects-field-error" role="alert">
            {String(form.formState.errors.authorId?.message ?? "")}
          </span>
        )}
      </div>
      <div className="metaobjects-field-array" key="syllabus">
        <span className="metaobjects-field-array-label">Syllabus</span>
        {syllabusArray.fields.map((item, index) => (
          <fieldset className="metaobjects-fieldset" key={item.id}>
            <legend className="metaobjects-fieldset-legend">Syllabus Section {index + 1}</legend>
            <div className="metaobjects-field" key="syllabus.${index}.heading">
              <label className="metaobjects-field-label">Heading</label>
              <input className="metaobjects-field-input" {...form.register(`syllabus.${index}.heading`)} />
            </div>
            <div className="metaobjects-field" key="syllabus.${index}.summary">
              <label className="metaobjects-field-label">Summary</label>
              <input className="metaobjects-field-input" {...form.register(`syllabus.${index}.summary`)} />
            </div>
            <div className="metaobjects-field" key="syllabus.${index}.orderIndex">
              <label className="metaobjects-field-label">Order Index</label>
              <input
                className="metaobjects-field-input"
                type="number"
                {...form.register(`syllabus.${index}.orderIndex`)}
              />
            </div>
            <button
              type="button"
              className="metaobjects-field-array-remove"
              onClick={() => syllabusArray.remove(index)}
            >
              Remove
            </button>
          </fieldset>
        ))}
        <button type="button" className="metaobjects-field-array-add" onClick={() => syllabusArray.append({} as never)}>
          Add Syllabus Section
        </button>
      </div>
      <fieldset className="metaobjects-fieldset" key="instructorProfile">
        <legend className="metaobjects-fieldset-legend">Instructor Profile</legend>
        <div className="metaobjects-field" key="instructorProfile.displayName">
          <label className="metaobjects-field-label">Display Name</label>
          <input className="metaobjects-field-input" {...form.register("instructorProfile.displayName")} />
        </div>
        <div className="metaobjects-field" key="instructorProfile.bio">
          <label className="metaobjects-field-label">Bio</label>
          <input className="metaobjects-field-input" {...form.register("instructorProfile.bio")} />
        </div>
        <div className="metaobjects-field" key="instructorProfile.credentialsSummary">
          <label className="metaobjects-field-label">Credentials Summary</label>
          <input className="metaobjects-field-input" {...form.register("instructorProfile.credentialsSummary")} />
        </div>
      </fieldset>
      <div className="metaobjects-form-actions">
        <button className="metaobjects-form-submit" type="submit" disabled={form.formState.isSubmitting}>
          {props.submitLabel ?? "Submit"}
        </button>
      </div>
    </form>
  );
}
