// @generated by @metaobjectsdev/codegen-ts — DO NOT EDIT.
// Source metadata: SubscriberBlurbPayload (SubscriberBlurbPayload)
// Extend in your own module (e.g. SubscriberBlurbPayload.extra.ts) — nothing imports it for you.
import { z } from "zod";

export interface SubscriberBlurbPayload {
  name?: string;
  status: Status;
}
export type Status = "active" | "paused" | "cancelled";
export const StatusEnum = z.enum(["active", "paused", "cancelled"]);
export const SubscriberBlurbPayloadInsertSchema = z.object({
  name: z.string().optional(),
  status: z.enum(["active", "paused", "cancelled"]),
});
