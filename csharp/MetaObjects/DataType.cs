namespace MetaObjects;

/// <summary>
/// Coarse value-type classification for type/subType pairs whose nodes carry a
/// typed value (field, attr). Placeholder — Task 1.1 will extend this.
/// </summary>
public enum DataType
{
    String,
    Int,
    Long,
    Double,
    Boolean,
    Date,
    Object,
}
